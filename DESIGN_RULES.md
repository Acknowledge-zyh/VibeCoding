# DESIGN_RULES.md — 「今日热搜」视觉设计规则

> 用途：给 AI 和学员共用的一份"可检查的视觉规则"。每条规则都写了**判定线**（能量出来的数字），
> 所以审查时不是"我觉得不太好看"，而是"DR-2 实测 1.68:1，低于 3:1，不合格"。
> 建立于 Day 9（2026-09-24）。与 PRD 5.2 的视觉要求一致，不新增 PRD 没提的功能。

## 一、规则清单

### DR-1 正文文字对比度 ≥ 4.5:1
所有承载信息的文字，与它实际所在背景的对比度不得低于 **4.5:1**（WCAG AA 正文标准）。
大字（≥24px，或 ≥18.66px 且加粗）可放宽到 3:1。
**判定线**：4.5:1。

### DR-2 界面元素对比度 ≥ 3:1
不是文字但需要被看见的东西——图标、星标、按钮描边、输入框边框、进度条——
与相邻背景的对比度不得低于 **3:1**（WCAG 非文字对比度）。
**判定线**：3:1。这条最容易被忽略：图标"淡淡的"看起来很高级，但用户找不到它。

### DR-3 交互三态必须可见，且键盘可用
每个可点击元素要有 **hover / focus / 明确选中态**：
- hover 不能只靠"几乎看不见的底色"，要么底色差可见，要么加描边/位移；
- 键盘 Tab 到它时必须有可见轮廓（`:focus-visible` 描边 ≥2px）；
- 键盘焦点顺序与视觉顺序一致。
**判定线**：hover 底色与常态对比 ≥1.3:1，且全部 `.btn`/按钮类有 `:focus-visible` 样式。

### DR-4 间距必须落在同一套刻度上
全站间距只用一套刻度：**4 / 8 / 12 / 16 / 24 / 32 / 48**（4 的倍数）。
同一类元素（如卡片内边距）必须用同一个值，不允许出现 6/10/14/18/22 这类"随手写的"数字。
**判定线**：间距取值种类 ≤7 种，且全部是 4 的倍数。

### DR-5 对齐成列，误差 0px
同一容器内，标题、列表项、按钮的**左内边距必须一致**，右边同理。
容器边缘（卡片、栏）左右外边距也必须一致。
**判定线**：同类元素左边缘坐标差 = 0px。

### DR-6 层级不超过 3 级，靠"字号 + 字重 + 颜色"三件套区分
一屏之内视觉层级最多 3 档（如：标题 / 主体 / 辅助）。
不能只靠颜色区分层级（色盲用户看不到）；字号阶梯至少相差 2px。
**判定线**：正文最小 13px（12px 仅用于辅助信息，且对比度仍须 ≥4.5:1）。

### DR-7 点击区 ≥ 40×40px
所有可点击元素的有效点击区不小于 **40×40px**（移动端手指友好），
相邻点击区之间的间距 ≥ 8px，避免误触。
**判定线**：每个可点击元素的 `getBoundingClientRect()` 宽高 ≥40。

### DR-8 圆角、描边、阴影三选一为主，全站统一
同一类容器用同一套"外观三件套"取值，不允许同层级卡片一个 8px 圆角一个 14px 圆角。
**判定线**：同层级容器圆角取值 ≤2 种，且有条目卡片/栏 = 同一值。

### DR-9 主色克制：同屏主色面积 < 10%
主色（品牌红 #d93025）只用于：层级标题的装饰块、第一优先级按钮、前 3 名序号、已收藏状态。
**判定线**：主色不用于大面积背景；页面里主色出现处 ≤5 类。

### DR-10 360px 起不横向溢出
最窄 360px 宽度下，`document.documentElement.scrollWidth` 不得超过视口宽度。
手机端单列纵向堆叠，长标题截断。
**判定线**：scrollWidth ≤ innerWidth。

### DR-11 加载中 / 空 / 错误三态必须可辨认
三种状态不能长得一样，差异至少体现在 **2 个维度**（文案 / 图形 / 颜色）。
错误态必须给出口（重新加载按钮）；三态都必须保留页面框架，绝不白屏。
**判定线**：三态的图标 + 文案 + 主色三者各不相同。

### DR-12 数字与长文本要有规矩
- 热度等数字用等宽数字（`font-variant-numeric: tabular-nums`），保证纵向对齐；
- 标题最多 2 行截断（手机 1 行），不允许把标题截成看不懂的半句；
- 超长英文/网址要能断行，不撑破容器。

## 二、审查方法（怎么把规则用起来）

1. 打开真实浏览器（本地 `python -m http.server 8000`）；
2. 用脚本 `scripts/audit-design.js` 一次性量出：每个元素的文字色 / 背景色 → 算对比度；间距取值集合；左边缘坐标；点击区尺寸；
3. 逐条对照判定线，把不达标的记成"问题清单"（要写实测数字）；
4. 只修不达标的，**不改版面结构**（Day 9 边界）；
5. 修完用同一脚本重量一遍，数字变了才算修好（不能只靠肉眼看）。

## 三、审查记录

见 `打卡/Day9-前端审查清单.md`（Day 9 产出）。修复后复测结论记在 AGENTS.md 第十九节之后。
