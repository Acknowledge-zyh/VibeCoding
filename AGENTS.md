# AGENTS.md — 项目规则文件

> 这是给 AI 助手看的"项目宪法"。每次开始干活前，AI 都要先读我、遵守我。
> 位置：放在项目根目录（工作区文件夹最外层），AI 才能自动找到。

## 一、这个项目是什么

- 项目名：VibeCoding 学习项目
- 目标：跟随 7 天课程，用 vibe coding 的方式做出第一个网页小项目
- 当前阶段：Day 1 —— 环境搭建与规则建立

## 二、沟通规则

1. 所有解释、注释、文档都用简体中文，零基础能听懂的话优先。
2. AI 每次改代码前，先用一两句话说明"打算改什么、为什么"。
3. 遇到我不懂的名词，AI 要用生活化的比喻解释，不要堆术语。
4. 我说错了方向时，AI 要直接指出来，不要顺着错的方向做。

## 三、写代码规则

1. 每次只做一小步：先让代码能跑起来，再谈优化（小步快跑）。
2. 每次改动后 AI 要告诉我：怎么运行、怎么验证改对了。
3. 不引入课程还没讲到的复杂框架，保持简单。
4. 代码里的注释用中文，解释"这段在干嘛"，方便我回头看。
5. 不确定的文件不要乱删乱改；动重要文件前先备份或提醒我。

## 四、文件与目录约定

- `打卡/` ：存放每天任务的截图（如环境验收截图）
- 项目代码从 Day 2 开始建立仓库后再放入
- 临时练习文件放 `练习/`（用到再建）

## 五、安全与边界

1. AI 不得把我的账号、密码、密钥写进任何文件或代码里。
2. AI 不得在没有我确认的情况下，删除或覆盖我已有的文件。
3. 涉及"发布到网上"的操作，必须先经过我确认。

## 六、今天（Day 1）的验收标准

- [x] WorkBuddy 可用
- [x] Git 可用（WorkBuddy 自带 git 2.55.0）
- [x] Node.js 可用（v22.22.2）
- [x] GitHub 账号已注册（2026-09-16）
- [x] 工作区文件夹已建好，且里面有本文件（AGENTS.md）
- [x] 能用自己的话说清：vibe coding 是什么、skill 是什么

## 七、Day 2 状态记录（2026-09-17）

- [x] Git 仓库初始化（main 分支），提交 52a2050、db1b9ce
- [x] index.html 占位页、.gitignore 忽略规则（含 .env/密钥/本地数据库）
- [x] 远程地址已预设：git@github.com:Acknowledge-zyh/VibeCoding.git
- [x] GitHub 网页建仓库（2026-09-18）
- [x] GitHub 添加 SSH 公钥（2026-09-18）
- [x] 首次推送 git push -u origin main（2026-09-18，main 已跟踪 origin/main）

## 八、Day 3 状态记录（2026-09-18）

- [x] 完成产品研究，产出 research.md（提交 b45f133、30e7676、0819c20）
- [x] 竞品比较：今日热榜、AnyKnew、微博热搜榜（页面结构 / 主要数据 / 用户动作 / 优点 / 不足）
- [x] 确定产品方向：从"聚合得最多"改为「少而快」——4 个平台、一屏看完、免登录
- [x] 定义 28 天 MVP 范围与「本期不做」9 项清单
- [x] 列出公开参考资料 8 条与 5 项待核实信息
- [ ] Day 4：编写 PRD（下一步）
- 说明：本日不写任何代码，符合任务边界

## 九、Day 4 状态记录（2026-09-19）

- [x] 完成 PRD.md（提交见当日记录）：定位、用户故事、F1–F4 功能需求、页面结构、非功能需求、B1–B10 验收标准
- [x] AI 自检：发现 Q1–Q3 三个「必须修改」级问题（一屏定义矛盾、刷新频率未定、降级缺失），已当场修复并入文档；结论为无未处理的必须修改项
- [ ] Day 5：技术选型（下一步）
- 说明：本日不做技术选型、不写代码，符合任务边界

## 十、Day 4 补充（2026-09-19 v2）

- [x] PRD.md 升级为 v2：MVP 新增「详情查看」与「本地收藏 + 备注」；登录、复杂推荐、付费明确列为后续功能
- [x] 每个功能均按「用户动作 → 可观察结果」表述；数据字段分为热搜条目与本地收藏记录两组
- [x] 验收标准更新为 B1–B12；自检 Q4–Q5 已修复，无未处理的必须修改项

## 十一、Day 5 状态记录（2026-09-20）

- [x] 完成 TECH_DESIGN.md：技术路线（原生前端 + Node/Express 小后端 + 无数据库 + 公开数据源）、各层分工、数据流图与六步说明、目录规划、方案变化影响面表、技术栈科普附录
- [x] 数据流一句话：平台公开来源 → 后端取回统一缓存 → 前端一次请求渲染 → 用户收藏写入浏览器本地存储（不回传）
- [x] 技术层面本期不做：框架/构建工具、数据库、SSR、自动化测试、容器化
- [ ] Day 6：静态页面骨架（MVP v0 方向）

## 十二、Day 5 补充（v2 技术方案，2026-09-20）

- [x] TECH_DESIGN.md 升级为 v2：新增三套方案比较（纯静态直连 / 自建小服务 / 云开发全托管）与默认路线推荐
- [x] 默认路线变更：由「自建 Node/Express」改为「CloudBase 静态托管 + Node.js 云函数」，数据库本期不用、二期启用 CloudBase PostgreSQL
- [x] 分三阶段落地：① Day 6–7 骨架 + 静态托管上线（A1/A2）② Day 8–14 接云函数拿真实数据（B1/B2/B8/B10）③ Day 15–28 交互打磨与全量上线（B3–B7/B9/B11/B12）
- [x] 补齐必写章节：项目结构、数据模型（HotItem / HotResponse / FavoriteItem / 二期 favorites 表）、API 列表（/api/hot、/api/health、二期 4 个）、前后端数据流九步、错误处理四层、环境变量清单、迁移注意事项 7 类
- [x] 取舍结论：前端本期不上 React/Vite（单页面、留迁移路径）；环境创建时必须选 PostgreSQL 类型（官方说明数据库类型事后不可混用）；云函数替代 Express（免运维、支撑 B12）
- [ ] Day 6：静态页面骨架（MVP v0 方向）

## 十三、待核实清单（延续）

- [ ] 4 个平台公开数据来源的可用性与合规（**Day 8 开始前必须确认**，见 TECH_DESIGN.md 第 0.1 节 Q4 与第 16 节）
- [ ] CloudBase 环境是否已开通、环境 ID、地域、数据库类型（影响二期 PG 是否可用，见 Q1/Q2）
- [ ] 云函数 HTTP 访问域名格式与跨域配置方式（阶段 2 联调前，见 Q3 与第 6.4 节）
- [ ] 免费额度当前口径与续期规则（开通当天，见第 10.8 节）
- [ ] 课程/老师是否强制使用 React/Vite 示范路线（见 Q5 与第 3.3 节取舍一）

## 十四、Day 5 补充（v3 技术方案，2026-09-20）

- [x] TECH_DESIGN.md 升级为 v3：候选方案收敛为 A/B 两套，按【学习成本 / 线上持久化 / 费用 / 排错难度】四维比较，**推荐方案 B（CloudBase 云开发全托管）**
- [x] 新增第 0.4 节「我的已知限制（电脑 / 时间 / 账号）」：中文用户名路径坑、Node/Git 零安装成本、CLI 安装权限未知、22 端口被拦走 443、28 天课程时间紧、腾讯云实名与支付为方案 B 硬门槛
- [x] 新增独立的第 10 章「部署」：部署前检查清单、阶段 1 只上静态页（**Day 7 首部署**）、阶段 2 加云函数、阶段 3 全量与回滚、每次改代码的四步流程、部署失败对照表、域名与续期
- [x] 数据对象与字段（HotItem / HotResponse / HotCache / FavoriteItem / 二期 favorites 表）独立成第 5 章；API 列表、数据流、错误处理、环境变量、迁移注意事项齐备
- [x] 降级条件已写明：若无法实名或无法绑支付方式，**退回方案 A**（静态样本 + 前端直连可用接口 + 本地收藏），并如实标注
- [ ] Day 6：静态页面骨架（MVP v0 方向，阶段 1 开始）

## 十五、待核实清单（v3 新增）

- [ ] 电脑能否全局安装 npm 包（CloudBase CLI），否则走控制台上传（Q9）
- [ ] 每天实际可投入时间（Q10），据此调整三阶段切分
- [ ] 腾讯云能否实名 / 能否绑支付方式（Q11，方案 B 的硬门槛）
- [ ] 电脑是否长期开机（Q12，决定能否以自建服务兜底）

## 十六、Day 6 追加规则（2026-09-21，只追加不重写）

> 以下三条为 Day 6 追加，原有第一至五节的规则一字未动。

### 16.1 任务边界（Day 1 应有、今日补回的规则）

1. **当天只做当天任务：学员没说「进入下一板块」，AI 就不越界**——不提前做明天的板块、不顺手"优化"今天的任务清单之外的东西。
2. 每天收尾必须：列出改动文件清单并说明各属于哪天的任务 → 提交推送（commit 标题以「Day X｜」开头）→ **停下等学员确认**，确认前不开始任何新任务。

### 16.2 学员自己追加的新规则（Day 6）

3. **提交前必须自检改动范围**：AI 在每次 `git add` 之前，先执行 `git status`，如果出现本日任务清单之外的意外改动（哪怕只是多了一个文件），必须先报告并说明来源，经学员同意才能提交——**防的是"顺手把不该入库的东西（临时文件、截图、密钥）一起推上 GitHub"这种情况**。
4. 防泄密铁律升级：`.env`、任何密钥、`打卡/` 目录**永远不进仓库**；AI 每次推送后要顺手确认这些文件仍未被跟踪（用 `git ls-files` 抽查）。

### 16.3 追加原因说明

- 16.1 补回的是课程 Day 1 的关键边界规则：历史上 AI 曾在"Day 2 收尾"后立刻开始规划 Day 3 的内容，边界感需要明文约束。
- 16.2 第 3 条是本次新加：Day 2–5 的推送里虽然实测都没出过事，但属于"没出事 ≠ 有防护"，提前把闸门立好。
- 16.2 第 4 条是把 Day 2 已实践的做法（推送后抽查敏感文件）从"习惯"升格为"规则"，防止以后换环境时被遗忘。

## 我的个人规则

- 回答、文档、代码注释全部使用简体中文，零基础能听懂优先。
- 项目所有代码和文档都放在根目录（VibeCoding）里；`打卡/` 文件夹只放截图，AI 不要改动里面的任何文件。
- 每次改完页面代码（index.html 等），提醒我刷新浏览器看效果，并告诉我这次改动应该看到什么变化。

## 十七、Day 7 状态记录（2026-09-22）

- [x] MVP v0 完成：index.html（主页面：顶栏 + 四列板块 + 详情弹层 + 收藏抽屉）、styles.css、app.js、data/hot.json（本地样本 4×10 条，结构对齐 HotResponse）
- [x] 功能实现：F1 四列榜单、F2 详情弹层（Esc/点外部关闭）、F3 本地收藏+备注（hot_favorites_v1，≤50 字）、F4 手动+10 分钟自动刷新、F5 桌面四列/移动端单列
- [x] 运行验证：python -m http.server 8000，页面与资源全部 HTTP 200；运行命令已存档 README.md
- [x] **首次部署完成（2026-09-22）**：CloudBase 环境 `acknowledge-d9gnqrpy89f1f7d21`（体验版，有效期至 2027-03-22），dist 4 文件上传成功；线上地址 https://acknowledge-d9gnqrpy89f1f7d21-1493626656.tcloudbaseapp.com/ ；线上验证 4 资源全部 HTTP 200、样本数据 4 平台 40 条完整

## 十八、Day 7 部署准备（2026-09-22，路线已确认：方案 B）

- [x] 用户已拍板部署路线：**CloudBase 云开发全托管（方案 B）**
- [x] Q9 实测有答案：本机 npm 10.9.7 可安装 CloudBase CLI（装在 Node 托管工作区，tcb 命令就绪）——部署可走 CLI，不必控制台手工上传
- [x] 部署准备完成：`dist/` 部署目录（已加入 .gitignore，不入库）、`scripts/sync-dist.js` 同步脚本、`cloudbaserc.json` 配置模板（envId 待填）、README 新增部署章节
- [x] **等待用户**：① 腾讯云实名 ✅ ② 开通 CloudBase 环境 ✅（环境 ID：acknowledge-d9gnqrpy89f1f7d21，体验版至 2027-03-22）③ envId 已填入 cloudbaserc.json ✅
- [x] tcb login 授权成功（CLI 3.8.3）；`tcb hosting deploy dist` 部署成功；线上 4 资源 HTTP 200、数据完整——**项目正式上线** 🎉
