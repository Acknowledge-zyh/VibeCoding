# 审查报告 — Day 9（2026-09-24）

> 依据 `DESIGN_RULES.md`，用 `scripts/audit-design.js` 在真实浏览器（本地 http://127.0.0.1:8000/）实测。
> 修复前后的整窗截图（含地址栏）在 `打卡/状态截图/Day9-修复前-*.png` 与 `Day9-修复后-*.png`。

## 一、问题清单（修复前实测）

| # | 规则 | 位置 | 修复前实测 | 判定线 | 结论 |
|---|---|---|---|---|---|
| 1 | DR-2 界面元素对比度 | 收藏星标 `#c4c8cf` | **1.68:1** | ≥3:1 | ❌ 收藏是核心功能，星标却几乎看不见 |
| 2 | DR-2 界面元素对比度 | 按钮/输入框描边 `#e6e8eb` | **1.23:1** | ≥3:1 | ❌ 顶栏两个按钮看着像纯文字 |
| 3 | DR-1 文字对比度 | 辅助文字 `#6b7280` | 4.83:1 | ≥4.5:1 | ⚠️ 擦线通过，但 12px 小字偏淡 |
| 4 | DR-3 交互态 | 条目 hover `#f7f8fa` | **1.04:1** | ≥1.2:1 | ❌ 悬停肉眼无反应 |
| 5 | DR-3 交互态 | 键盘焦点样式 | 38 个可点元素只有 **1** 条规则 | 全覆盖 | ❌ Tab 键找不到落点 |
| 6 | DR-5 对齐 | 栏头内边距 14px vs 条目 6px | 左边缘差 **8px** | 0px | ❌ 序号和栏名不在一条线上 |
| 7 | DR-4 间距刻度 | 全页间距取值 | **15 种**（1/2/5/6/10/14/18/20/22…） | ≤7 种且为 4 的倍数 | ❌ |
| 8 | DR-6 字号阶梯 | 12/13/14/16/17/18/19/20 | **8 档**，12→13 只差 1px | 相邻差 ≥2px | ❌ 层级靠猜 |
| 9 | DR-7 点击区 | 顶栏按钮 | 高 **37px** | ≥40px | ❌ |
| 10 | DR-8 圆角 | 弹层 14px / 卡片 12px / 收藏卡 10px | 4 种 | 同层级 ≤2 种 | ⚠️ |

通过项：DR-10（360px 无横向溢出）、DR-11（三态可辨）、DR-12（等宽数字 + 两行截断）在修复前已达标，未动。

## 二、修复对照（修复后复测）

| # | 改法 | 修复后实测 | 结果 |
|---|---|---|---|
| 1 | 星标色 `#c4c8cf` → `#7d8694` | **3.68:1** | ✅ |
| 2 | 新增 `--line-strong: #868f9c`，按钮/输入框描边改用 | **3.27:1** | ✅ |
| 3 | 辅助文字 `--ink-2` `#6b7280` → `#5b6472` | **5.98:1** | ✅ |
| 4 | hover 底色 → `--hover: #e2e7ed`；已收藏行加专属 hover | **1.24:1** | ✅ |
| 5 | 给 `.btn` / `.item-star` / `.modal-close` 补 `:focus-visible`（2px 品牌色描边） | 38/38 覆盖，实测 outline solid 2px | ✅ |
| 6 | 栏头与条目统一为 16px 左内边距（栏 16 = 列表容器 8 + 条目 8） | 左边缘差 **0**，右边缘差 **0** | ✅ |
| 7 | 全站间距收进 `--sp-1..7`（4/8/12/16/24/32/48） | 取值 **6 种**，全部在刻度上 | ✅ |
| 8 | 字号收进 `--fs-xs/base/lg/xl/2xl`（12/14/16/18/20） | **5 档**，相邻差全部 = 2px | ✅ |
| 9 | `.btn` 加 `min-height: 40px`；弹层关闭按钮 36→40px | 高度不足 40px 的：无 | ✅ |
| 10 | 卡片圆角统一 12px、行/按钮 8px | 同层级 2 种 | ✅ |

移动端回归：375 / 360px 单列堆叠、无横向溢出；四状态（加载中/空/错误/正常）、收藏交互、键盘焦点全部复测通过。

## 三、今天改的几处里，哪一处对「像不像一个正经产品」影响最大？

**收藏星标（1.68:1 → 3.68:1）。**
理由：收藏（PRD F3）是这个产品除"看"以外唯一的核心动作，但修复前它的入口是一个几乎和背景融为一体的浅灰星——用户根本发现不了"这产品能收藏"。正经产品和玩具的差别往往就在这里：**核心操作一定一眼可见**。按钮描边（1.23:1 → 3.27:1）是同一个问题的另一面，排第二。

## 四、方法沉淀（余力加练：把视觉规则写成可复用约束）

本次把 DR-4 间距刻度、DR-6 字号阶梯、DR-1/2/3 的颜色全部写成 `styles.css` 顶部的设计令牌（`--sp-*`、`--fs-*`、`--ink-*`、`--line-strong`、`--hover`）。
以后改任何间距/字号/颜色，**只能引用令牌**——想引入一个不在刻度上的值，必须先改令牌定义，等于给"随手写数字"上了一道闸。这就是"一条视觉规则 = 一条组件约束"的落地方式。
