# TECH_DESIGN.md —「今日热搜」技术方案

> Day 5 产出｜撰写日期：2026-09-20｜撰写人：Acknowledge-zyh
> 上游文档：research.md（Day 3）、PRD.md v2（Day 4）
> 本文档回答：数据从哪来、到哪去，用什么技术路线实现。**不写代码、不改 PRD。**

---

## 1. 一句话说清数据从哪来、到哪去

> **数据从 4 个平台的公开热榜来源来，经我们自己的 Node 后端定时取回并统一格式、暂存在缓存里，再由前端页面一次请求全部取回、渲染成一屏四列；用户的收藏与备注只写进浏览器的本地存储，不回传服务器。**

拆成两句话就是：

- **热搜数据（读）**：平台 → 我们的后端（取回+统一+缓存）→ 前端页面 → 用户的眼睛
- **收藏数据（写）**：用户在页面上的操作 → 浏览器本地存储 → 明天打开还在（不出这台设备）

---

## 2. 技术路线（一行式总览）

| 层 | 选什么 | 主要职责 | 为什么选它 |
|----|-------|---------|-----------|
| **前端** | 原生 HTML + CSS + JavaScript（不用框架） | 渲染一屏四列列表、详情弹层、收藏抽屉、刷新交互 | 零基础最容易看懂和调试；PRD 只有一个页面，上框架属于过度设计（React/Vue 的学习成本会挤掉本期时间） |
| **后端** | Node.js + Express（一个小服务，只提供 1 个接口） | 定时取回 4 个平台的热榜数据、统一字段、缓存、对外提供 `/api/hot` | **必须有后端**：浏览器直接抓各平台会被跨域（CORS）和反爬拦住；后端做"数据中转站"最稳。Express 是 Node 生态里最简单的 Web 框架，十几行就能起服务 |
| **数据存储** | 浏览器 localStorage（收藏与备注）+ 后端内存/JSON 文件缓存（热榜） | 收藏免登录保存；热榜缓存避免频繁请求被限流 | **本期不引入数据库**：PRD 明确没有账号体系、不做云端同步，收藏存本地就够；上数据库只会增加复杂度（数据库课程第 23 天才讲） |
| **数据来源** | 各平台公开热榜数据来源（优先公开接口，其次公开榜单页） | 提供原始热搜条目 | 免费、无需自建爬虫集群；**合规与可用性待核实**（见第 6 节） |
| **部署** | 静态前端 + Node 服务一起上线（部署平台待 Day 22 选定） | 让页面可长期访问 | 一个服务同时托管前端与接口，运维最简单 |
| **版本管理** | Git + GitHub（已就绪） | 提交、回溯、发布记录 | Day 2 已配好，直接复用 |

**路线一句话**：原生前端 + 一个 Node 小后端 + 无数据库（收藏存浏览器本地）+ 公开数据源。

### 2.1 为什么不选另外两类方案（一句话说明）

- **纯前端直连平台接口**：省掉后端，但浏览器跨域与反爬会让页面经常空着——直接违反 PRD 的 B1「40 条真实数据」。
- **上框架 + 数据库 + 云函数全家桶**：能力过剩，零基础在 28 天内更难排错，也违反 PRD 的"本期不做"边界。

---

## 3. 各层分工（用生活化的说法）

- **前端 = 餐厅大堂**：用户看得见、点得着的部分。负责把数据摆好看，接住用户的每一次点击（点条目、点收藏、点刷新）。
- **后端 = 后厨**：用户看不见的干活环节。定时去 4 个平台"进货"，把不同来源的菜统一装盘（字段对齐），先放在备菜台（缓存）上，等前端来取。
- **数据库 = 仓库**：本期**不建仓库**。因为热榜数据是"看一眼就够"的时鲜，不需要长期囤；用户的收藏太少太私人，放用户自己的浏览器抽屉里最合适。

---

## 4. 数据流图

```
                   ┌──────────────────────────────────────────────┐
   数据从哪来 →     │  ① 微博   ② 百度   ③ 知乎   ④ 抖音           │
                   │      （各平台公开热榜来源）                    │
                   └───────────────────┬──────────────────────────┘
                                       │ 定时取回（间隔 ≥ 10 分钟）
                                       ▼
                   ┌──────────────────────────────────────────────┐
                   │  后端（Node + Express）                       │
                   │  ② 统一字段：平台/排名/标题/链接/热度/时间      │
                   │  ③ 缓存备菜台（内存或 JSON 文件）              │
                   └───────────────────┬──────────────────────────┘
                                       │ 提供 1 个接口：GET /api/hot
                                       ▼
                   ┌──────────────────────────────────────────────┐
                   │  前端页面（一个页面）                         │
                   │  ④ 一次请求取回 40 条 → 渲染一屏四列           │
                   │  ⑤ 详情弹层 / 刷新按钮 / 收藏抽屉              │
                   └───────────┬────────────────────┬─────────────┘
                               │                    │
              用户的眼睛 ←──────┘                    │ ⑥ 点收藏 / 写备注
              （看榜单、点原文跳原平台）              ▼
                                       ┌──────────────────────────┐
                                       │  浏览器本地存储 localStorage│
                                       │  收藏 + 备注（只在本机）    │
                                       └──────────┬───────────────┘
                                                  │ 下次打开仍在
                                                  ▼
                                          数据到哪去 → 用户自己的屏幕
```

### 4.1 数据流六步说明

| 步骤 | 发生什么 | 数据形态 |
|------|---------|---------|
| ① 取回 | 后端每 ≥10 分钟向 4 个平台的数据来源要一次榜单 | 原始数据（各平台格式不同） |
| ② 统一 | 后端把不同来源映射成同一套字段（PRD 第 6.1 节） | 统一结构：platform/rank/title/url/heat/time |
| ③ 缓存 | 统一后的结果暂存在后端（内存或 JSON 文件） | 一份 40 条的"当前榜单" |
| ④ 取用 | 前端加载时请求一次 `GET /api/hot`，拿到全部 40 条 | 一次请求，四列同源 |
| ⑤ 展示 | 前端渲染四列列表；点条目出详情弹层 | 用户可见的页面 |
| ⑥ 写入 | 用户点收藏、写备注 → 存进浏览器 localStorage | 收藏记录（含 note、saved_at），不回传服务器 |

### 4.2 两个"绝不"

- 热搜数据**绝不**在前端直接向平台要（会被跨域和反爬拦住）——统一走后端中转。
- 收藏数据**绝不**回传服务器（本期无账号体系，避免任何用户数据上云）。

---

## 5. 文件与目录规划（Day 6 起使用）

```
VibeCoding/
├─ AGENTS.md              规则文件（Day 1）
├─ research.md            产品研究（Day 3）
├─ PRD.md                 产品需求（Day 4）
├─ TECH_DESIGN.md         技术方案（Day 5，本文件）
├─ index.html             主页面（Day 6 起替换 Day 2 的占位页）
├─ styles.css             样式（Day 6）
├─ app.js                 前端交互：渲染、弹层、收藏、刷新（Day 6）
├─ server.js              后端：取数据 + /api/hot 接口（Day 8 起）
├─ data/hot.json          本地数据样本（Day 6 先用它做静态版）
├─ .env                   （以后放配置，已在 .gitignore，绝不入库）
└─ 打卡/                   截图（不入库）
```

---

## 6. 数据来源与合规（待核实，沿用 research.md 结论）

| 序号 | 待确认事项 | 为什么重要 | 何时确认 |
|------|-----------|-----------|---------|
| 1 | 4 个平台是否存在公开可用的数据接口、是否需要授权 | 决定第 2 周能否打通真实数据（PRD B1） | Day 8 开始前 |
| 2 | 聚合展示"标题 + 链接"的合规边界 | 决定产品能否合法上线 | Day 8 开始前 |
| 3 | 请求频率上限（是否支持 10 分钟级刷新而不被限流） | 决定缓存策略与刷新间隔 | 第 2 周开发时 |

**降级备案**：若某平台无可用公开来源，则该平台先显示"暂时无法获取"（PRD 第 7 节既有设计），并优先保证其余平台可用。

---

## 7. 方案变化影响面（余力加练）

| 如果方案变成… | 受影响的文件 | 影响程度 |
|--------------|-------------|---------|
| 平台从 4 个改为 6 个 | `server.js`（数据源列表）、`app.js`（列布局）、`styles.css`（栅格断点）、`PRD.md`（范围与验收） | 中 |
| 收藏改为云端同步（引入登录） | 新增后端接口与数据库、`app.js`（读写改接口）、`PRD.md`（范围大改）、`TECH_DESIGN.md`（新增数据库层） | 大 |
| 前端改用 Vue/React 框架 | `index.html`、`app.js` 全部重写、构建配置新增 | 大 |
| 数据源从公开接口换成第三方付费 API | `server.js`（换数据源 + 加密钥到 .env）、`.gitignore`（确认 .env 已忽略）、`PRD.md`（成本说明） | 小 |
| 部署平台更换 | 新增部署配置，`TECH_DESIGN.md` 第 2 节更新 | 小 |
| 刷新间隔从 10 分钟改为 1 分钟 | `server.js`（定时与缓存策略）、数据源限流风险上升 | 小 |

**结论**：把数据源的获取集中写在 `server.js` 一处，前端只认 `/api/hot` 一种格式——这样上面大多数变化都被"关在"后端里，前端不用动。

---

## 8. 附录：技术栈是什么（余力加练科普）

**技术栈**＝做这个产品用到的所有技术的组合，像"盖房子用的材料清单"。网页/App 常见分层与本期取舍：

| 层 | 常见技术 | 有什么用 | 优势 | 劣势 | 本期用吗 |
|----|---------|---------|------|------|---------|
| 结构 | HTML | 页面的骨架 | 简单、必备 | 单靠自己不好看 | ✅ |
| 样式 | CSS | 颜色、字号、布局 | 控制外观 | 大项目难维护 | ✅ |
| 交互 | 原生 JavaScript | 点击、弹层、存收藏 | 零依赖、易调试 | 复杂项目代码会乱 | ✅ |
| 框架 | React / Vue | 大型界面的组件化开发 | 生态大、好维护 | 学习成本高、需要构建工具 | ❌ 本期不用 |
| 后端 | Node.js + Express | 提供接口、处理数据 | 与前端同一种语言（JS） | 需要服务器 | ✅ |
| 后端（其他） | Python + Flask/Django、Java + Spring | 同上 | 生态成熟 | 零基础要多学一门语言 | ❌ 本期不用 |
| 数据库 | MySQL / MongoDB / SQLite | 长期存数据 | 能存海量数据 | 要设计表结构、要运维 | ❌ 本期不用 |
| 本地存储 | 浏览器 localStorage | 存小量数据（收藏） | 免登录、零成本 | 换设备就没有 | ✅ |
| 版本管理 | Git + GitHub | 记录每次修改、可回退 | 必备技能 | 需要学基本命令 | ✅ |

**一句话**：本期技术栈＝ HTML + CSS + 原生 JS + Node/Express + localStorage + Git；够用、好懂、不多学一样东西。

---

## 9. 技术层面的"本期不做"

- 不做前端构建工具链（Webpack/Vite）、不用框架
- 不引入数据库、不做账号与鉴权
- 不做服务端渲染（SSR）、不做多页面路由
- 不做自动化测试框架（本期靠人工按验收标准逐条点）
- 不做容器化/CI-CD（Day 28 前按需再说）

---

## 10. 与 PRD 验收标准的对应

| PRD 验收项 | 本方案如何支撑 |
|-----------|--------------|
| B1 40 条真实数据 | 后端 ① 取回 + ② 统一（第 4 节） |
| B2 首屏 ≤2 秒 | 后端先缓存好，前端一次请求取全量（③④） |
| B3–B4 详情与跳原文 | 前端弹层 + url 字段直接跳转（⑤） |
| B5–B7 收藏、备注、本地持久化 | localStorage 读写（⑥，第 4.2 条"绝不回传"） |
| B8 刷新 | 后端定时 ≥10 分钟；前端手动刷新按钮 |
| B10 单平台失败降级 | 后端按平台分别容错，接口返回"哪几家成功" |
| B11 无登录无广告 | 无账号体系、无第三方广告脚本 |
