# TECH_DESIGN.md —「今日热搜」技术方案（v2）

> Day 5 产出｜初稿：2026-09-20｜v2 修订：2026-09-20｜撰写人：Acknowledge-zyh
> 上游文档：research.md（Day 3）、PRD.md v2（Day 4）
> **v2 变更**：① 新增三套候选方案的横向比较与默认路线推荐；② 技术路线由「自建 Node/Express 服务」改为「腾讯云开发 CloudBase 全托管（静态托管 + Node 云函数）」，数据库由「本期不用」改为「本期不用、二期启用 PostgreSQL」；③ 补齐项目结构、数据模型、API 列表、前后端数据流、错误处理、环境变量、迁移注意事项七个必写章节。
> 本文档**不写代码、不改 PRD**（PRD 仅被引用）。

---

## 0. 动手之前：信息不足清单与临时假设

你要求「信息不足先列问题再给临时假设」，所以这一节放在最前面。**下面 8 个问题我无法自己查证**（涉及你的账号、课程要求、预算），先列出，随后给出本方案的临时假设——假设一旦被推翻，我会更新对应章节。

### 0.1 需要你确认的 8 个问题

| 编号 | 问题 | 为什么影响设计 | 现状 |
|------|------|--------------|------|
| Q1 | 是否已有腾讯云账号并完成实名认证？ | CloudBase 环境必须实名后才能开通；没有账号则第 2 周无法接数据 | ⏳ 未知 |
| Q2 | CloudBase 环境是否已开通？环境 ID、地域、**数据库类型选的是「云数据库（文档型）」还是「PostgreSQL」**？ | 官方说明：环境创建时的数据库类型**事后不可混用**——选文档型就不能用 PostgreSQL；新加坡地域只支持 PostgreSQL（来源见第 15 节） | ⏳ 未知 |
| Q3 | 是否已安装 CloudBase CLI 并登录？ | 决定部署是「命令行一键」还是「控制台上传」，两种操作的写法完全不同 | ⏳ 未知 |
| Q4 | 4 个平台（微博/百度/知乎/抖音）是否有可用、合规的数据来源？ | 沿用 research.md 待核实项；**这是全项目最大风险**，决定 B1「40 条真实数据」能否达成 | ⏳ 待核实（Day 8 前） |
| Q5 | 老师/课程是否**强制**使用示范路线（React/Vite）？ | 若强制，第 3.3 节的「前端不上构建工具」结论要改为「Day 12 前完成 React 化」 | ⏳ 未知 |
| Q6 | 是否有预算？（免费体验版额度、个人版套餐） | 决定能否启用 PostgreSQL 与是否长期租用 | ⏳ 未知 |
| Q7 | 是否需要自定义域名？域名是否已备案？ | 自定义域名需域名 + 国内备案；决定「同域部署（免跨域）」这条路走不走得通 | ⏳ 未知 |
| Q8 | 收藏是否必须云端同步？ | 若「必须」，PostgreSQL 与登录就要提前到本期，PRD 第 3.2 节需要重议 | ⏳ 未知（PRD 现定为本地） |

### 0.2 在拿到答案前，本文档采用的临时假设

| 编号 | 临时假设 | 影响的章节 | 若假设被推翻 |
|------|---------|-----------|-------------|
| T1 | 你将新开通一个 CloudBase 环境，**数据库类型选 PostgreSQL**（上海地域） | 第 3、5.4、10.3 节 | 若选文档型：5.4 节表结构改为集合（JSON），10.3 节迁移脚本相应调整 |
| T2 | 本阶段先用**控制台 + CLI** 手动部署，不做 CI 自动构建 | 第 4、10.1 节 | 若上 Git 自动构建：第 4 节需增加构建配置文件 |
| T3 | 前端在 Day 6–Day 21 用**原生 HTML/CSS/JS**，暂不引入 Vite/React | 第 3.3、10.2 节 | 若必须上 React：按 10.2 节迁移清单执行 |
| T4 | 收藏本期存**浏览器本地**，不落库、不登录（沿用 PRD v2） | 第 5.3 节 | 若改为云端：启用 5.4 节表 + 6.3 节接口 |
| T5 | 4 个平台**至少能拿到 2 个**可用公开来源，其余按降级处理 | 第 8 节 | 若全部拿不到：需改数据来源（可选第三方聚合 API，要密钥与费用） |
| T6 | 免费额度足够本项目 28 天内使用（本地开发 + 小流量访问） | 第 3.4、10.7 节 | 若额度不足：需评估个人版套餐或降低刷新频率 |
| T7 | 无需自定义域名，直接用平台提供的默认域名上线 | 第 8.5、10.5 节 | 若需自定义域名：预留备案时间（国内通常按周计） |
| T8 | 页面与接口**不同域**，需要显式处理跨域（CORS） | 第 8.2、6.4 节 | 若配置了同域自定义域名：CORS 一条可以删掉 |

### 0.3 全文标注规则

- ✅ **已核实**：有官方来源（第 15 节列出）。
- 🔶 **临时假设**：依赖 0.2 节的假设才成立。
- ⏳ **待核实**：需要你在控制台/课程里确认，或在 Day 8 前确认。

---

## 1. 一句话说清数据从哪来、到哪去

> **数据从 4 个平台的公开热榜来源来，经云函数定时取回、统一格式并缓存，前端页面一次请求全部取回、渲染成一屏四列；用户的收藏与备注只写进浏览器本地存储，不回传服务器。**

两条链分开看：

- **读链（热搜）**：平台 → 云函数（取回 + 统一 + 缓存）→ 前端页面 → 用户的眼睛
- **写链（收藏）**：用户点击 → 浏览器本地存储 → 下次打开还在（不出这台设备）

> 二期（若启用云端收藏）写链会变成：用户点击 → 云函数 → PostgreSQL → 换设备也在。

---

## 2. 三套候选方案比较

### 2.1 比较维度

按「前端 / 后端 / 数据库 / 部署」四件套比较，再看四个零基础最关心的指标：**上手难度、出错概率、"能上线"的保障、以后能不能长大**。

### 2.2 三套方案各自是什么

| 方案 | 一句话 | 四件套 |
|------|--------|--------|
| **方案 A：纯静态直连** | 只有一个网页，浏览器直接向平台要数据 | 前端：原生页或 Vite+React｜后端：无｜数据库：无（localStorage）｜部署：静态托管（CloudBase 或 GitHub Pages） |
| **方案 B：自建小服务** | 原生网页 + 自己写一个 Node 服务当"数据中转站"，跑在自己电脑或一台服务器上（Day 5 初稿路线） | 前端：原生 HTML/CSS/JS｜后端：Node + Express 单接口｜数据库：无（localStorage）｜部署：需要一台一直开着的机器 |
| **方案 C：云开发全托管**（示范路线） | 网页托管在云上，数据获取交给云函数，需要存数据时用云数据库 | 前端：原生页或 Vite+React｜后端：CloudBase Node.js 云函数｜数据库：本期不用 / 二期 CloudBase PostgreSQL｜部署：CloudBase 静态网站托管（自动 HTTPS + CDN） |

### 2.3 四维对照总表

| 维度 | 方案 A 纯静态直连 | 方案 B 自建小服务 | 方案 C 云开发全托管 |
|------|-----------------|-----------------|-------------------|
| **前端** | 原生或 React/Vite 都行 | 原生（够用） | 原生起步，需要时再上 Vite/React |
| **后端** | 无 | Node + Express（约 20 行起步） | Node.js 云函数（Serverless，免运维）✅ |
| **数据库** | 无，收藏在浏览器 | 无，收藏在浏览器 | 本期不用；二期 PostgreSQL（含 RLS 行级权限、自动生成 RESTful 接口）✅ |
| **部署** | 静态托管，最简单 | **必须自己运维**：开机=在线，关机=下线；HTTPS 与域名自己配 | 静态托管一键部署，自动 HTTPS + 全球 CDN，支持版本回滚 ✅ |
| **能不能拿到真实数据（PRD B1）** | ❌ 浏览器直连会被跨域与反爬拦住，页面大概率空白 | ✅ 服务端出网，能拿数据 | ✅ 云函数是服务端，能拿数据 |
| **"链接能长期访问"（PRD B12）** | ✅ 托管平台保证 | ❌ 依赖你这台电脑开着 | ✅ 平台保证 |
| **上手难度（零基础）** | ★☆☆☆☆ 最低 | ★★★☆☆ 中（要理解服务、端口、开机运行） | ★★★☆☆ 中（要理解环境、环境 ID、CLI 部署） |
| **出错概率与排错难度** | 低，但**失败原因难懂**（跨域报错） | 中（本地好调，上线后环境变了会懵） | 中（配置类错误信息不友好，但官方有 CLI/MCP 辅助）🔶 |
| **第二次修改后的重复部署成本** | 极低（重新上传） | 高（要重启服务、换机器要重装环境） | 低（一条 CLI 命令） |
| **以后"长大"的空间** | 几乎为零 | 中（要自己加数据库、加服务器） | 大（同一个环境里加数据库、认证、AI 能力） |
| **钱** | 免费或极低 | 需要一台机器（或长期开着自家电脑） | 免费体验版可起步，超额需套餐 🔶（额度⏳） |
| **结论** | 只能当"骨架期"的临时方案 | 适合本地练手，不适合交付 | **推荐作为默认路线** |

### 2.4 零基础视角的打分（5 分制，分越高越好）

| 指标 | A 纯静态 | B 自建服务 | C 云开发 | 说明 |
|------|---------|-----------|---------|------|
| 第一周能跑起来的速度 | 5 | 4 | 4 | 都能在 Day 7 出骨架（骨架阶段其实三套没差别，都是静态页面） |
| 出问题时能自己看懂报错 | 4 | 3 | 2 | 云开发的配置类报错最需要 AI/文档帮忙 🔶 |
| 满足 PRD 硬指标（B1/B12） | 1 | 3 | 5 | A 基本不可能达成 B1 |
| 28 天后的可延展性 | 1 | 2 | 5 | C 可以在同一环境里继续加登录、AI 能力 |
| 长期稳定不折腾 | 3 | 2 | 5 | C 不用管机器 |

### 2.5 三套方案"会在哪里卡住"（提前拆弹）

| 方案 | 最可能卡住的点 | 拆弹办法 |
|------|--------------|---------|
| A | 页面打开一片空白，控制台报 CORS / 403 | 直接放弃 A，换 C（这不是你的代码问题，是浏览器安全策略） |
| B | 本地跑得好好的，一关机朋友就打不开；换台电脑环境又要重装 | 只把 B 当"本地验证用"，上线改用 C |
| C | 卡在"环境怎么建、环境 ID 填哪、部署命令报什么错" | 三步走：① 先只做静态托管上线（Day 7，验证链路）；② 再加云函数（Day 8+）；③ 最后才碰数据库（二期）。**一次只加一层** |

---

## 3. 推荐默认路线与取舍

### 3.1 结论

**推荐方案 C（CloudBase 全托管）作为默认路线，但分三阶段启用，每一阶段单独验收。**

理由一句话：**只有方案 C 能同时满足 PRD 的两条硬指标**——B1（拿到 40 条真实数据）需要服务端出网，B12（链接可长期访问）需要托管平台保证，而 C 用同一个平台一次性解决这两件事，还免去了运维。

### 3.2 三阶段落地表

| 阶段 | 时间 | 前端 | 后端 | 数据库 | 部署 | 对应验收 |
|------|------|------|------|--------|------|---------|
| **阶段 1：骨架上线** | Day 6–Day 7 | 原生 HTML/CSS/JS，数据用本地样本 `data/hot.json` | 不接 | 不用 | **只做静态托管**，先把"网页能打开、链接能访问"跑通 | A1、A2（Day 7） |
| **阶段 2：真实数据** | Day 8–Day 14 | 原生页，改为请求 `/api/hot` | **接 Node 云函数**：取回 4 平台数据、统一字段、缓存、定时刷新 | 不用 | 云函数 + 静态托管 | B1、B2、B8、B10 |
| **阶段 3：体验与可选云端** | Day 15–Day 28 | 交互打磨（详情弹层、收藏抽屉、移动端） | 云函数保持只读 + 缓存 | **默认仍不用**；若要做云端收藏再启用 PostgreSQL | 全量部署 + 版本回滚 | B3–B7、B9、B11、B12 |

**每一阶段的验收标准不一样，绝不跳阶段**（清单见 2.5 节最后一行）。

### 3.3 三个关键取舍（这就是"取舍"部分）

**取舍一：前端为什么本期不上 React/Vite？**
- 本项目**只有一个页面 + 两个浮层**（PRD 第 5.1 节）。React/Vite 解决的是"页面多、状态复杂、多人协作"的问题——本项目没有这些痛点。
- 上 Vite/React 会多出三件事：装依赖、跑构建、把「源码目录」和「托管目录」分开（部署时要传 `dist/` 而不是源码）。对零基础来说，**每一件都是一处新的踩坑点**。
- 因此本期先不上，但**留好升级路径**（10.2 节写了完整迁移清单）。若 Q5 的答案是"老师强制"，就在 Day 12 前后按该清单迁移——代价是前端文件重写一遍，后端与接口契约**完全不用动**（这也是为什么先把接口契约固定下来）。

**取舍二：数据库为什么本期不启用，但要预留在 PostgreSQL 上？**
- PRD 明确定：没有登录、收藏存本地（D3 节 + 第 3.2 节后续功能）。**没有账号体系，数据库存什么都无从归属**——表建了也是空的。
- 但环境一旦创建，数据库类型就定了（官方说明：文档型与 PostgreSQL 不可在同一环境混用）✅。所以**开通环境时就要替二期做决定**：选 PostgreSQL，二期可直接加表使用，不用重建环境。
- 代价：PostgreSQL 版环境在新加坡地域是唯一选项、在上海地域属可选；具体计费差异 ⏳ 需在控制台确认（见 Q2/Q6）。

**取舍三：为什么用云函数，而不是 Day 5 初稿里的自建 Express？**
- 两者的**接口契约完全一样**（都是 `GET /api/hot`），区别只在"跑在哪"。
- 自建 Express 要你保证机器常开、自己配 HTTPS 与域名；云函数把这三件事都交给平台（自动扩缩容、自动 HTTPS、免运维）✅。
- 保留自建 Express 的唯一场景：**本地开发期为了快速调试**——本地起个 Express 对着 `data/hot.json` 调，调试完把同一段逻辑搬进云函数即可（这也是 10.1 节的迁移注意事项）。

### 3.4 与 Day 5 初稿（提交 b042db9）的差异说明

| 项目 | v1 初稿（Day 5 上午） | v2 现稿 | 为什么改 |
|------|---------------------|---------|---------|
| 后端 | 自建 Node + Express | CloudBase Node 云函数（本地仍可先用 Express 调试） | 免运维 + 自动 HTTPS + 支撑 B12 |
| 部署 | "部署平台待 Day 22 选定" | CloudBase 静态网站托管（Day 7 就先上） | 把"能上线"提前验证，避免 Day 28 才发现部署卡住 |
| 数据库 | 本期不用（未提方案） | 本期不用，但**环境选 PostgreSQL 类型**为二期预留 | 数据库类型事后不可混用，必须提前定 ✅ |
| 前端 | 原生（结论相同） | 原生 + 明确升级路径与迁移清单 | 补上"以后怎么改" |
| 新增章节 | — | 项目结构 / 数据模型 / API 列表 / 错误处理 / 环境变量 / 迁移注意事项 | 本次要求 |

---

## 4. 项目结构

```
VibeCoding/                         ← 仓库根目录，也是 Git 仓库
├─ .gitignore                       ✅ 已存在（含 .env、密钥、*.db、打卡/）
├─ AGENTS.md                        Day 1 规则文件
├─ research.md                      Day 3 产品研究
├─ PRD.md                           Day 4 产品需求（v2）
├─ TECH_DESIGN.md                   Day 5 技术方案（本文件）
├─ index.html                       主页面（Day 6 起替代 Day 2 的占位页）
├─ styles.css                       样式（Day 6）
├─ app.js                           前端交互：渲染 / 详情弹层 / 收藏 / 刷新（Day 6）
├─ data/
│   └─ hot.json                     本地样本数据（Day 6 静态版用；阶段 2 后仅作兜底）
├─ cloudfunctions/                  ⏳ 阶段 2 才创建（云函数根目录）
│   └─ hot/                         "热搜中转"云函数（取数 + 统一 + 缓存 + /api/hot）
│       ├─ index.js                 入口
│       ├─ sources.js               4 个平台的数据来源适配（换源只改这里）
│       ├─ normalize.js             统一字段
│       └─ package.json
├─ cloudbaserc.json                 ⏳ CloudBase 部署配置（环境 ID、函数根目录、托管目录）
├─ .env                             ✅ 已在 .gitignore —— 本地配置（含敏感值），**绝不入库**
└─ 打卡/                            ✅ 已在 .gitignore —— 每日截图
```

**入库 vs 不入库（重要）**：

| 内容 | 入库？ | 说明 |
|------|-------|------|
| 上面除 `.env`、`打卡/` 之外的文件 | ✅ 入库 | 源码与文档都要有版本记录 |
| `.env`、任何密钥文件 | ❌ 绝不入库 | `.gitignore` 已覆盖（Day 2 已实测验证） |
| `node_modules/`、构建产物 `dist/` | ❌ 不入库 | 依赖与产物都可重新生成 |
| 本地样本 `data/hot.json` | ✅ 可入库 | 里面是公开标题与链接，不含个人信息 |

> 🔶 **v1 里规划的 `server.js` 不再需要**（它属于"自建服务"路线）。若阶段 2 前你想本地先调通，可临时建 `server.js` 做开发用，**上线前删除或明确标注"仅本地开发"**，避免以后混淆。

---

## 5. 数据模型

**一句话原则**：前端只认一种数据结构，后端负责把 4 个平台的不同格式"翻译"成这一种。**数据的形状一旦定下就不许随便改**（前端、云函数、缓存、将来的数据库表都依赖它）。

### 5.1 接口数据模型（前端与云函数之间的契约）

**热搜条目 HotItem**（严格对齐 PRD 第 6.1 节）：

| 字段 | 类型 | 说明 | 必填 |
|------|------|------|------|
| platform | string | 平台名，取值：`微博` / `百度` / `知乎` / `抖音` | 是 |
| rank | number | 排名 1–10 | 是 |
| title | string | 标题（前端负责截断显示，后端不做截断） | 是 |
| url | string | 原文链接（http/https 开头） | 是 |
| heat | string \| null | 热度值原样保留（有则显示，无则 `null`，**不显示 0**） | 否 |
| time | string \| null | 上榜/更新时间（有则显示） | 否 |

**聚合响应 HotResponse**（`GET /api/hot` 的返回体）：

```json
{
  "updated_at": "2026-09-20T10:40:00+08:00",
  "platforms": [
    { "name": "微博", "ok": true,  "message": null,                  "updated_at": "2026-09-20T10:40:00+08:00" },
    { "name": "百度", "ok": true,  "message": null,                  "updated_at": "2026-09-20T10:40:00+08:00" },
    { "name": "知乎", "ok": false, "message": "暂时无法获取，稍后自动重试", "updated_at": null },
    { "name": "抖音", "ok": true,  "message": null,                  "updated_at": "2026-09-20T10:40:00+08:00" }
  ],
  "items": [
    { "platform": "微博", "rank": 1, "title": "示例标题", "url": "https://example.com/a", "heat": "1234567", "time": "10:30" }
  ]
}
```

**设计要点**：
- `platforms[].ok` 是**降级开关**：前端按它决定哪个板块显示列表、哪个板块显示"暂时无法获取"（满足 PRD 第 7 节与验收项 B10）。
- `items[]` 只放成功的条目，**最多 40 条**（4×10）。某平台失败时其余平台照常返回。
- `updated_at` 是**整份数据的时间戳**，前端顶栏显示的"更新于 HH:MM"取它；平台各自的时间放 `platforms[].updated_at`。

### 5.2 缓存模型（云函数内，防限流的关键）

| 项目 | 设计 | 理由 |
|------|------|------|
| 缓存位置 | 云函数内共享变量（实例存活期间复用）🔶 | 零成本、零依赖；实例回收后重新拉一次不影响体验 |
| 缓存键 | `hot:4-platforms` | 只有一个数据集合，键固定即可 |
| 缓存有效期 | `CACHE_TTL_MINUTES`，默认 **10 分钟** | 对齐 PRD F4「自动更新间隔 ≥10 分钟」，同时降低被平台限流的风险 |
| 过期行为 | 有旧数据时**先返回旧的**，后台再更新（"先给人看，再去进货"） | 保证 B2「首屏 ≤2 秒」永远成立 |
| 兜底 | 更新失败仍返回旧缓存 + 旧时间戳 | 对齐 PRD「绝不显示空白页」 |

> ⏳ 若云函数实例被频繁回收导致每次都重新拉数据（可能触发限流），升级方案是把缓存写进数据库或云存储——**这属于阶段 3 的备选项，本期先不做**。

### 5.3 收藏记录（本期：浏览器 localStorage）

**FavoriteItem**（严格对齐 PRD 第 6.2 节）：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 前端生成（如 `时间戳 + 平台 + 排名`），用于更新与删除 |
| platform | string | 收藏时从原条目带入 |
| title | string | 收藏时从原条目带入 |
| url | string | 收藏时从原条目带入 |
| note | string | 用户备注，**≤50 字**，可空、可改、可删 |
| saved_at | string | 收藏时间（ISO 格式），收藏列表按此倒序 |

**存储约定**：
- 存成一个数组，键名例如 `hot_favorites_v1`（**带版本号**，将来结构升级时可识别旧数据）。
- 写入时机：点收藏 / 改备注 / 删备注 / 取消收藏，每次都整体覆盖写回。
- 隐私红线：**不含任何身份信息**，不回传服务器（对应 PRD 6.2 节说明）。

### 5.4 数据库表（二期预留：CloudBase PostgreSQL）

本期不建表、不连库。以下是**二期做"收藏云端同步"时**要用的结构，提前写好是为了让接口与字段在今天就对齐：

```sql
-- 二期启用；本期不执行
create table if not exists favorites (
  id         bigserial primary key,
  user_id    text        not null,          -- 二期登录后填用户标识；本期无登录，字段先留着
  platform   text        not null,
  title      text        not null,
  url        text        not null,
  note       varchar(50),
  saved_at   timestamptz not null default now(),
  unique (user_id, platform, title)          -- 同一用户对同一条热搜只留一条，天然防重复收藏
);

create index if not exists idx_favorites_user_saved
  on favorites (user_id, saved_at desc);     -- 对齐"按收藏时间倒序"的读取方式
```

| 设计点 | 说明 |
|--------|------|
| 为什么用 `unique(user_id, platform, title)` 而不是带 url | 原文链接常带跟踪参数，同一条热搜的 url 可能变；标题 + 平台更稳定 |
| 行级权限（RLS） | CloudBase PostgreSQL 自带 RLS ✅：二期"只能读写自己那几行"用 SQL 策略表达，不必自己写后端鉴权逻辑 |
| 接口自动化 | 建表后可自动生成 RESTful 接口（CRUD 即用）✅，二期接口可能不用手写云函数 |
| 迁移注意 | 见 10.3 节（本地收藏数据怎么搬上云，不能让用户白收藏） |

---

## 6. API 列表

### 6.1 本期唯一必需接口

| 项目 | 内容 |
|------|------|
| 接口 | `GET /api/hot` |
| 谁调用 | 前端页面（打开时、点刷新时） |
| 作用 | 返回 4 个平台的 Top 10 聚合数据 + 各平台成功状态 |
| 请求参数 | 无（第一阶段不接受任何参数，减少攻击面） |
| 成功响应 | `200` + HotResponse（见 5.1） |
| 失败响应 | `500` + `{ "error": { "code": "UPSTREAM_ALL_FAILED", "message": "暂时无法获取数据" } }` |
| 缓存策略 | 服务端缓存 10 分钟；命中缓存直接返回 |
| 部署形态 | CloudBase Node.js 云函数，通过平台提供的 HTTP 访问地址对外 🔶 |

> 🔶 前端要访问的**完整地址**（形如 `https://<环境ID>.service.tcloudbase.com/api/hot`）以控制台「HTTP 访问服务 / 云函数 URL 化」页面实际显示为准 ⏳。建议把它写成前端一处常量（如 `app.js` 顶部 `const API_BASE`），换环境只改一行。

### 6.2 运维自检接口

| 项目 | 内容 |
|------|------|
| 接口 | `GET /api/health` |
| 作用 | 排查用：返回 `{"ok":true,"time":"...","cache_age_seconds":123}`，一眼看出服务活着、缓存多旧 |
| 为什么需要 | 页面空白时，先敲这个地址就能判断"是服务挂了还是前端写错了"——**排错提速最划算的一个接口** |

### 6.3 二期接口（本期不实现，仅登记）

| 接口 | 作用 | 备注 |
|------|------|------|
| `GET /api/favorites` | 取当前用户的收藏列表 | 需登录（二期） |
| `POST /api/favorites` | 新增一条收藏（含备注） | 命中 `unique` 约束时返回已存在 |
| `PATCH /api/favorites/:id` | 修改备注 | 备注 >50 字返回 400 |
| `DELETE /api/favorites/:id` | 取消收藏 | — |
| `GET /api/hot?refresh=1` | 强制立即刷新（绕过 10 分钟缓存） | ⏳ 需限流（如每 IP 每分钟 1 次），否则可能被平台封 |

### 6.4 接口约定（所有接口通用）

| 约定 | 内容 |
|------|------|
| 统一包裹 | 成功直接返回数据对象；失败返回 `{ "error": { "code", "message" } }` |
| 错误码 | `UPSTREAM_TIMEOUT`（上游超时）、`UPSTREAM_ALL_FAILED`（全挂）、`BAD_REQUEST`（参数不合法）、`RATE_LIMITED`（刷太勤）、`INTERNAL`（未知错误） |
| 跨域（CORS） | 🔶 页面与接口不同域，云函数需返回允许来源的响应头；上线前必须实测一次"从页面能否请求成功" |
| 超时 | 云函数读取上游的超时设 `FETCH_TIMEOUT_MS`（默认 8000ms），**超时即降级，不许挂住整个接口** |
| 版本 | 本期路径不带版本号；将来若改结构，用 `/api/v2/hot` 而不是原地改（保护已上线的页面） |

---

## 7. 前后端数据流

```
   数据从哪来 →  ┌───────────────────────────────────────────────┐
                │  微博    百度    知乎    抖音                  │
                │  （各平台公开热榜来源，格式各不相同）           │
                └───────────────────┬───────────────────────────┘
                                    │ ① 定时取回（间隔 ≥10 分钟）
                                    ▼
                ┌───────────────────────────────────────────────┐
                │  CloudBase Node.js 云函数（服务端，用户看不见） │
                │  ② 统一字段：platform/rank/title/url/heat/time │
                │  ③ 缓存 10 分钟（过期先返旧、后台再更新）       │
                │  ④ 按平台分别记录成功/失败状态                 │
                └───────────────────┬───────────────────────────┘
                                    │ GET /api/hot（一次请求取全量）
                                    ▼
                ┌───────────────────────────────────────────────┐
                │  前端页面（托管在 CloudBase 静态托管）          │
                │  ⑤ 渲染一屏四列；失败的板块显示降级提示         │
                │  ⑥ 详情弹层 / 刷新 / 收藏抽屉                  │
                └───────────┬─────────────────────┬─────────────┘
                            │                     │
           用户的眼睛 ←──────┘                     │ ⑦ 点收藏 / 写备注
           （看榜单、点原文跳原平台）                ▼
                                    ┌───────────────────────────┐
                                    │  浏览器本地存储 localStorage │
                                    │  收藏 + 备注（只在本机）    │
                                    └──────────┬────────────────┘
                                               │ 下次打开仍在
                                               ▼
                                       数据到哪去 → 用户自己的屏幕
```

### 7.1 数据流九步说明

| 步骤 | 发生什么 | 数据形态 | 谁负责 |
|------|---------|---------|--------|
| ① 取回 | 云函数按 ≥10 分钟节奏向各平台来源要榜单 | 原始数据（各平台格式不同） | 云函数 |
| ② 统一 | 映射成 HotItem 一套字段 | 统一结构（5.1 节） | 云函数 |
| ③ 缓存 | 统一后的 40 条暂存 10 分钟 | 一份"当前榜单" | 云函数 |
| ④ 标记状态 | 记录哪几家成功、哪家失败及原因 | `platforms[]` 状态数组 | 云函数 |
| ⑤ 传输 | 前端请求一次 `/api/hot`，一次拿全 | HotResponse | 接口 |
| ⑥ 渲染 | 四列并排渲染；失败板块显示提示 | 用户可见页面 | 前端 |
| ⑦ 交互 | 点条目出详情弹层、点刷新重新请求 | 页面状态 | 前端 |
| ⑧ 写入 | 收藏与备注写入浏览器本地存储 | FavoriteItem（5.3 节） | 前端 |
| ⑨ 持久 | 下次打开从本地读回收藏 | 同上 | 前端 |

### 7.2 三条"绝不"

- 热搜数据**绝不**由前端直接向平台索取（跨域 + 反爬）——一律经云函数中转。
- 收藏数据**绝不**回传服务器（本期无账号体系，不收集任何用户数据）。
- 密钥**绝不**出现在前端代码或前端环境变量里（前端打包后一切可见）。

---

## 8. 错误处理

**总原则**：**单点失败不许影响整页。** 一个平台挂了，其他三个照常；数据晚到，显示旧数据也不许白屏。

### 8.1 数据源层（云函数内部）

| 情况 | 处理 | 用户看到 |
|------|------|---------|
| 某平台请求超时（> 8s） | 记为失败，放弃该平台，继续处理其余 | 该板块"暂时无法获取，稍后自动重试" |
| 某平台返回 403/404/429 | 同上；429 则**拉长该平台的刷新间隔**（避免越封越死） | 同上 |
| 某平台页面结构变了导致解析为空 | 视为失败，**绝不返回空板块冒充成功** | 同上 |
| 4 个平台全部失败 | 返回旧缓存 + 旧时间戳；若无缓存则返回 500 | 有缓存：页面正常但时间戳是旧的；无缓存：整页提示"暂时无法获取" |
| 拿到数据但字段缺失（如 heat 为空） | 缺失字段置 `null`，其余照常 | 该条不显示热度，不显示"0" |

### 8.2 云函数与接口层

| 情况 | 处理 |
|------|------|
| 冷启动慢（首次调用需唤醒） | 前端显示 skeleton 占位，不显示空白；🔶 视实测决定是否需要"预热" |
| 跨域被拦（CORS） | 云函数返回允许来源响应头；上线前必须实测一次 |
| 被刷（同一 IP 高频调用） | 返回 `429 RATE_LIMITED`；前端提示"操作太频繁，稍后再试" |
| 未捕获异常 | 统一兜底：记日志 + 返回 `500 INTERNAL`，不外泄内部堆栈信息 |

### 8.3 前端层

| 情况 | 处理 | 对应 PRD |
|------|-----|---------|
| 请求失败或超时 | 显示已缓存在页面的上次数据 + 原时间戳；提供"重试"按钮 | 第 7 节 |
| 某板块降级 | 只该板块显示提示文案，**其余板块完整可用** | B10 |
| 本地存储不可用（隐私模式/被禁） | 点收藏时提示"当前浏览器环境不支持保存收藏"，浏览功能不受影响 | 第 7 节 |
| 收藏的条目已下榜、原文 404 | 收藏记录照常显示，不做处理 | 第 7 节 |
| 手机竖屏 | 板块纵向单列、无横向滚动；弹层全屏 | B9 |

### 8.4 人（你自己）也会出错 —— 三层排查法

| 症状 | 第一步查哪里 | 判断依据 |
|------|-------------|---------|
| 页面整片空白 | 敲 `GET /api/health` | 返回 `ok:true` → 问题在前端；返回错误 → 问题在云函数 |
| 某板块一直失败 | 看云函数日志里该平台的错误 | 403/429 是反爬或限流；解析空是页面结构变了 |
| 部署后还是旧页面 | 看 CDN 缓存与版本记录 | 静态托管有版本与回滚能力 ✅，可回滚验证 |

### 8.5 部署与域名

| 情况 | 处理 |
|------|------|
| 访问域名打不开 | 先用平台默认域名验证，再排查自定义域名（默认域名无需备案）🔶 |
| 页面 404（刷新子路径） | 只有单页应用才有这问题；本期单页面不受影响 🔶 |
| 免费额度用尽 / 环境到期 | 见 10.7 节：**提前续期**，否则环境可能被回收 |

---

## 9. 环境变量与密钥管理

### 9.1 铁律三条

1. **前端不放任何密钥**：前端代码与前端环境变量（如 Vite 的 `VITE_` 前缀变量）打包后会全部暴露给访问者。前端只需要**一个非敏感的接口地址**。
2. **敏感值只存两处**：① 本地开发时存项目根的 `.env`（**已确认被 `.gitignore` 覆盖，不会入库**）；② 线上存**云函数的云端环境变量**（控制台或 `cloudbaserc.json` 配置，不落仓库）。
3. **`.env` 一旦误提交，改值也晚了**——要立刻在源头把该密钥作废重发（Git 历史里删不干净）。

### 9.2 变量清单

| 变量名 | 用在哪 | 示例 | 敏感？ | 存哪 |
|--------|-------|------|-------|------|
| `TCB_ENV_ID` | CloudBase CLI 部署、`cloudbaserc.json` | `env-xxxxxxxx` | 否（配置项） | `cloudbaserc.json`（可入库） |
| `CACHE_TTL_MINUTES` | 云函数 | `10` | 否 | 云端环境变量 |
| `FETCH_TIMEOUT_MS` | 云函数 | `8000` | 否 | 云端环境变量 |
| `API_BASE` | 前端 | `https://<环境ID>.service.tcloudbase.com` | 否 | 前端常量（**不是密钥**）🔶 |
| 各平台来源地址/接口名 | 云函数 `sources.js` | 按平台而定 | 否 | 代码内常量 |
| **第三方数据 API 的 Key**（若使用） | 云函数 | `sk-xxxx` | **是** | 云端环境变量（**绝不入库、绝不进前端**） |
| **`DATABASE_URL` / `PG_CONNECTION_STRING`**（二期启用 PG 时） | 二期云函数 | `postgresql://user:pwd@host:5432/db` | **是** | 云端环境变量 |
| `PORT` | 本地调试用的 Express（仅阶段 2 前） | `3000` | 否 | 本地 `.env` |

### 9.3 `.gitignore` 现状（已实测通过，无需改动）

已覆盖 `.env`、`.env.*`、`*.pem`、`*.key`、`secrets*.json`、`credentials*.json`、`*.db`、`node_modules/`、`打卡/`。Day 2 曾用三个假文件（`.env`、`test.db`、`secrets.json`）实测全部被拦截 ✅。

> ⏳ 待你确认：若将来使用第三方付费 API，建议在 `.env` 里再加一组 `SOURCE_*_KEY` 占位，并补一条 CI 检查（如提交前自查 `git status` 里有没有 `.env`）。本期不需要。

---

## 10. 迁移注意事项

> 这一节回答"如果方案变成别的样子，会不会推倒重来"。结论：**不会，因为接口契约（5.1 节）与数据来源（`sources.js`）都被隔离在固定位置。**

### 10.1 自建 Express → CloudBase 云函数

| 事项 | 做法 |
|------|------|
| 什么不变 | 接口路径 `/api/hot`、返回结构、缓存策略**全部不变**（这正是先定契约的价值） |
| 什么要改 | 入口写法：Express 的 `app.get(...)` → 云函数的"入口函数 + 事件参数"；读参数与返回格式的写法不同 |
| 配置差异 | Express 用 `PORT` 监听端口；云函数由平台调度，**没有端口概念**，改为配置 HTTP 触发路径 |
| 缓存差异 | Express 进程常驻，内存缓存可靠；**云函数实例可能被回收**，缓存不保证常驻 → 触发频繁拉取时改用数据库/云存储缓存（见 5.2 节备注） |
| 依赖 | `express` 不再需要；保留 `axios`/`node-fetch` 之类的请求库即可 |
| 上线步骤 | 本地调通 → 上传云函数 → 控制台配 HTTP 触发 → 把前端 `API_BASE` 改成云端地址（**改一行**） |

### 10.2 原生前端 → Vite + React

| 事项 | 做法 |
|------|------|
| 触发条件 | ① 老师强制要求；② 页面数量明显变多（超过 3 个视图）；③ 交互状态复杂到原生难以维护。**三条都不满足就别迁** |
| 目录变化 | `index.html` 移到根、`app.js` 拆成 `src/` 下若干组件；静态资源进 `public/` 或 `src/assets/` |
| 托管目录变化 | **部署时要传 `dist/`（构建产物），不是源码目录** —— 这是最容易卡住的一步 |
| 状态管理 | 收藏列表从"直接读写 localStorage"抽成一个自定义 Hook，避免每个组件重复写存取逻辑 |
| 接口调用 | 把 `API_BASE` 收进一个 `api.js`，组件只调函数，不关心地址 |
| 路由 | 本期无多页面；将来若加路由，静态托管需把未知路径回退到 `index.html` ✅ |
| 工作量预估 | 前端文件基本重写一遍；**云函数、接口、数据模型、错误处理都不用动** |
| 迁移顺序 | 先跑通"空壳能构建能部署" → 再逐个搬组件 → 最后删掉旧文件（保留 Git 历史随时回退） |

### 10.3 本地收藏 → 云端收藏（启用 PostgreSQL）

| 事项 | 做法 |
|------|------|
| 前置条件 | 必须先有登录/匿名身份（否则 `user_id` 无从取值）——这也是 PRD 把"登录"列为后续功能的原因 |
| 第一步 | 在环境里启用 PostgreSQL（**环境创建时已选 PG 类型则可直接用**，否则需新建环境并迁移静态托管与云函数配置）🔶 |
| 第二步 | 按 5.4 节建表、加索引、配 RLS（行级权限） |
| 第三步 | 写迁移脚本：把浏览器里的 `hot_favorites_v1` 数组读出来，逐条 POST 到新接口，**成功后本地保留一份只读备份**（防止迁移失败丢数据） |
| 第四步 | 去重：靠 `unique(user_id, platform, title)` 兜住；插入冲突按"更新备注"处理 |
| 第五步 | 前端改为"优先读云端、云端不可用时回退本地"，并给用户一句提示"收藏已同步到云端" |
| 回退方案 | 接口出问题时仍可退回纯本地收藏——**本地存储的代码不要删**，留作降级路径 |

### 10.4 换数据来源（最常见的变更）

| 事项 | 做法 |
|------|------|
| 只改一处 | 所有来源的获取与解析集中在 `cloudfunctions/hot/sources.js`；换源只改这个文件 |
| 契约不变 | 只要最终还是输出 HotItem，**前端一行都不用改** |
| 密钥 | 新源如需 Key → 进云端环境变量，不进代码 |
| 验证 | 改完后必须重跑 PRD 的 B10（模拟单平台失败）与 B1（40 条真实数据） |

### 10.5 域名与 HTTPS

| 事项 | 做法 |
|------|------|
| 默认域名 | 先用平台提供的默认域名上线，无需域名与备案，自动 HTTPS ✅ |
| 自定义域名 | 需要域名 + 国内备案（按周计），**要提前排期**，不要拖到 Day 28 |
| 同域好处 | 若把页面与接口都挂到同一自定义域名下，可彻底摆脱跨域问题（8.2 节那条可以删） |

### 10.6 环境 ID 与配置漂移

| 事项 | 做法 |
|------|------|
| 环境 ID 变了 | 改 `cloudbaserc.json` + 前端 `API_BASE`，共 2 处 |
| 建议 | 环境 ID 单独写一行注释标明"换环境只改这里"，避免以后全局搜索 |

### 10.7 免费额度与续期（别在最后一天踩这个坑）

| 事项 | 做法 |
|------|------|
| 免费体验版 | 官方口径：每个云开发账号 1 个免费环境、每月 3,000 资源点、**单次续期 6 个月，到期前 1 个月内可续**✅；额度用尽或到期未续，环境可能停服乃至销毁 ⏳ |
| 行动项 | 开通当天就在日历里加一个"到期前 2 周续期"的提醒；Day 28 上线前再核一次额度余量 |
| 省钱小技巧 | 刷新间隔保持 ≥10 分钟（既满足 PRD，又省资源点）；静态托管流量尽量走 CDN 缓存 |

---

## 11. 方案变化影响面

| 如果方案变成… | 受影响的文件 | 影响程度 |
|--------------|-------------|---------|
| 前端改上 Vite + React | 前端文件重写、新增构建配置、托管目录改 `dist/`；后端与接口不动 | 中 |
| 收藏改为云端同步 | 环境启用 PG、新增 4 个接口、前端读写改造、PRD 需重议范围 | 大 |
| 数据库类型选成"文档型"而非 PostgreSQL | 5.4 节表结构改为集合（JSON）、10.3 节脚本调整、环境无需重建 | 小 |
| 数据源换成第三方付费 API | 只改 `sources.js` + 云端环境变量加 Key | 小 |
| 平台从 4 个改为 6 个 | `sources.js`、`app.js`（列布局）、`styles.css`（断点）、PRD 范围与验收 | 中 |
| 加登录注册 | 需要身份认证服务 + PG 表加 `user_id` 约束、前端多出登录界面、PRD 大改 | 大 |
| 换部署平台（如 Vercel / 自建服务器） | `cloudbaserc.json` 与部署命令、`API_BASE`；云函数需改为普通服务 | 中 |
| 刷新间隔由 10 分钟改为 1 分钟 | `CACHE_TTL_MINUTES` + 上游限流风险上升 | 小 |

**结论**：把"数据来源"锁在 `sources.js`、把"接口契约"锁在 5.1 节之后，**大部分变化都被关在云函数里**，前端与文档都不用动。

---

## 12. 技术栈科普（本项目的取舍一览）

**技术栈** = 做这个产品用到的所有技术组合，像"盖房子的材料清单"。

| 层 | 常见技术 | 有什么用 | 优势 | 劣势 | 本项目 |
|----|---------|---------|------|------|--------|
| 结构 | HTML | 页面骨架 | 必备、简单 | 单靠自己不好看 | ✅ 用 |
| 样式 | CSS | 排版、配色、响应式 | 控制外观 | 大项目难维护 | ✅ 用 |
| 交互 | 原生 JavaScript | 渲染、弹层、收藏、刷新 | 零依赖、易调试 | 复杂页面会乱 | ✅ 用 |
| 框架 | React / Vue（+ Vite 构建） | 组件化开发复杂界面 | 生态大、易维护 | 学习与构建成本高 | ⏸️ **本期不用**，留升级路径（10.2） |
| 后端 | Node.js + Express | 提供接口、处理数据 | 与前端同语言 | 需自己运维 | 🔶 仅本地调试用 |
| 后端（托管） | **CloudBase Node.js 云函数** | 取数、统一、缓存、出接口 | 免运维、自动扩缩容、自动 HTTPS | 冷启动、配置类报错不友好 | ✅ 本期主用 |
| 后端（其他语言） | Python / Java / Go | 同上 | 生态成熟 | 零基础要多学一门语言 | ❌ 不用 |
| 数据库 | MySQL / MongoDB / SQLite | 长期存数据 | 能存海量与关系型数据 | 要设计表、要运维 | ❌ 本期不用 |
| 数据库（云） | **CloudBase PostgreSQL** | 云端存收藏、支持 RLS 与自动接口 | 环境创建即得、兼容标准 SQL | 本期无账号体系用不上 | ⏸️ 二期启用（5.4、10.3） |
| 本地存储 | 浏览器 localStorage | 存收藏与备注 | 免登录、零成本、零隐私风险 | 换设备就没有 | ✅ 本期用 |
| 部署 | 静态托管（CloudBase） | 把网页发布到全网 | 免服务器、自动 HTTPS + CDN、可回滚 | 只适合静态资源 | ✅ 本期用 |
| 版本管理 | Git + GitHub | 记录修改、可回退 | 必备技能 | 要学基本命令 | ✅ 已就绪 |

**一句话**：本项目技术栈 = **HTML + CSS + 原生 JS + CloudBase 云函数 + CloudBase 静态托管 + localStorage + Git**；数据库（PostgreSQL）先"占好位置"，等有了登录再开。

---

## 13. 技术层面的"本期不做"

- 不做前端构建工具链（Vite/Webpack）、不上 React/Vue（3.3 节已说明，留迁移路径）
- 不引入数据库、不做账号与登录鉴权（PostgreSQL 仅预留结构，不建表不连库）
- 不做服务端渲染（SSR）、不做多页面路由
- 不做自动化测试与 CI/CD（本期靠人工按 PRD 验收标准逐条点）
- 不做容器化、不做自建服务器运维
- 不做数据落库归档（热榜是"看一眼就够"的时鲜数据，存下来价值低、成本高）

---

## 14. 与 PRD 验收标准的对应

| PRD 验收项 | 本方案如何支撑 |
|-----------|--------------|
| A1、A2 骨架结构（Day 7） | 阶段 1：原生页面 + 静态托管上线，用本地样本数据 |
| B1 40 条真实数据 | 阶段 2：云函数服务端取回 + 统一字段（第 7 节 ①②） |
| B2 首屏 ≤2 秒 | 服务端缓存 + 静态托管 CDN + 前端一次请求取全量（5.2、7.1 ⑤） |
| B3、B4 详情与跳原文 | 前端弹层 + `url` 字段直接跳转（HotItem.url） |
| B5–B7 收藏、备注、本地持久化 | localStorage 读写（5.3 节），且明确"不回传服务器" |
| B8 刷新 | `CACHE_TTL_MINUTES=10` 的缓存节奏 + 前端手动刷新（6.1、5.2） |
| B9 移动端无横向滚动 | 前端响应式布局（8.3 节） |
| B10 单平台失败降级 | `platforms[].ok` 状态 + 前端按板块降级（5.1、8.1、8.3） |
| B11 无登录无广告 | 无账号体系、无第三方广告脚本、不收集用户数据 |
| B12 陌生用户 30 秒看完 + 长期可访问 | 一屏四列布局 + 静态托管长期托管（3.2 阶段 3） |

---

## 15. 参考资料（全部为公开来源，访问日期 2026-09-20）

| 序号 | 资料 | 用到了什么 | 类型 |
|------|------|-----------|------|
| 1 | 腾讯云开发 CloudBase 官网 https://tcb.cloud.tencent.com/ | 平台能力概览（数据库 / 云函数 / 静态托管 / 身份认证 / AI）、套餐档位 | ✅ 已核实 |
| 2 | CloudBase 定价页 https://cloudbase.cloud.tencent.com/pricing | 免费体验版：1 个免费环境、3,000 资源点/月、单次续期 6 个月 | ✅ 已核实（具体权益⏳以购买页为准） |
| 3 | CloudBase 开发文档中心 https://docs.cloudbase.net/ | 数据库双引擎（PostgreSQL + 文档型）、云函数与静态托管能力、框架接入指引 | ✅ 已核实 |
| 4 | 腾讯云文档《云开发 CloudBase · 购买指南》https://cloud.tencent.com/document/buy-guide/876/127357 | **环境创建时可选数据库类型、不同地域支持差异、事后不可混用** | ✅ 已核实（本方案 3.3 取舍二的关键依据） |
| 5 | 腾讯云《静态网站托管》产品页 https://cloud.tencent.com/product/wh | 免费默认域名、自定义域名免费 SSL、强制 HTTPS、CDN 加速、CLI 部署 | ✅ 已核实（免费流量额度口径可能已变更 ⏳） |
| 6 | CloudBase 官方博客《PostgreSQL 支持》https://cloudbase.cloud.tencent.com/en/blog/2026/08/10/cloudbase-postgresql | PostgreSQL 版特性：`postgresql://` 直连、RLS、自动生成 RESTful 接口、兼容 Supabase SDK、数据库分支 | ✅ 已核实 |
| 7 | 腾讯云《云开发 CloudBase》产品页 https://cloud.tencent.com/product/tcb | 云函数/云托管形态、Web 应用托管（支持 React/Vue、内置 HTTPS 与 CDN、Git 自动构建） | ✅ 已核实 |
| 8 | 本项目 research.md、PRD.md v2 | 产品方向、MVP 范围、B1–B12 验收标准 | 本项目文档 |

**仍不确定、需人工核实的信息（⏳）**：

| 序号 | 不确定项 | 何时确认 |
|------|---------|---------|
| 1 | 4 个平台是否有可用、合规的公开数据来源（决定 B1 能否达成） | **Day 8 开始前**（最高优先级） |
| 2 | 你的 CloudBase 环境是否已开通、数据库类型与地域、环境 ID | 阶段 1 部署前 |
| 3 | 云函数 HTTP 访问的完整域名格式与跨域配置方式 | 阶段 2 开始前 |
| 4 | 免费额度的当前口径与续期规则（是否仍为 6 个月一续） | 开通当天 |
| 5 | 课程/老师是否强制使用 React/Vite 示范路线（影响 3.3 取舍一） | 尽早问老师 |
| 6 | 页面与接口是否同域（影响是否需要处理 CORS） | 阶段 2 联调时 |
| 7 | 云函数实例回收频率是否影响缓存命中（影响是否需要外部缓存） | 阶段 3 实测 |
