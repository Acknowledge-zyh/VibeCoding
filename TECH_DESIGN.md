# TECH_DESIGN.md —「今日热搜」技术方案（v3）

> Day 5 产出｜初稿：2026-09-20｜v2：2026-09-20｜v3：2026-09-20｜撰写人：Acknowledge-zyh
> 上游文档：research.md（Day 3）、PRD.md v2（Day 4）
> **v3 变更**：① 候选方案收敛为 **A / B 两套**，按【学习成本 / 线上持久化 / 费用 / 排错难度】四维比较并给出推荐；② 新增第 0.4 节「我的已知限制（电脑 / 时间 / 账号）」及限制对选型的影响；③ 新增独立的**第 10 章「部署」**（首次部署清单、每次改动流程、回滚、失败对照表）；④ 章号顺延。
> 本文档**不写代码、不改 PRD、不新增任何代码文件**（第 4 章列的是"待创建清单"，不是已建文件）。

---

## 0. 动手之前：信息不足清单、临时假设与我的已知限制

### 0.1 需要你确认的问题

| 编号 | 问题 | 为什么影响设计 | 现状 |
|------|------|--------------|------|
| Q1 | 是否已有腾讯云账号并完成实名认证？ | CloudBase 环境必须实名后开通；没有账号则第 2 周无法接真实数据 | ⏳ 未知 |
| Q2 | CloudBase 环境是否已开通？环境 ID、地域、**数据库类型选的是「云数据库（文档型）」还是「PostgreSQL」**？ | 官方说明：环境创建时的数据库类型**事后不可混用**；新加坡地域只支持 PostgreSQL（来源见第 16 节） | ⏳ 未知 |
| Q3 | 是否已安装 CloudBase CLI 并登录？ | 决定部署是「命令行一条命令」还是「控制台上传」，两种操作写法完全不同 | ⏳ 未知 |
| Q4 | 4 个平台（微博/百度/知乎/抖音）是否有可用、合规的数据来源？ | 沿用 research.md 待核实项；**全项目最大风险**，决定 PRD B1「40 条真实数据」能否达成 | ⏳ 待核实（Day 8 前） |
| Q5 | 课程/老师是否**强制**使用示范路线（React/Vite）？ | 若强制，第 3.3 节取舍一改为「Day 12 前完成 React 化」 | ⏳ 未知 |
| Q6 | 是否有预算？ | 决定能否长期启用 PostgreSQL | ⏳ 未知 |
| Q7 | 是否需要自定义域名？域名是否已备案？ | 自定义域名需域名 + 国内备案；决定"同域部署免跨域"能否走通 | ⏳ 未知 |
| Q8 | 收藏是否必须云端同步？ | 若"必须"，登录与 PostgreSQL 要提前到本期，PRD 第 3.2 节需重议 | ⏳ 未知（PRD 现定为本地） |
| **Q9** | 你的电脑**能否全局安装 npm 包**（即有没有管理员权限、能不能跑 `npm install -g`）？ | CloudBase CLI 通常靠 npm 全局安装；装不了就得走控制台上传这条替代路 | ⏳ 未知 |
| **Q10** | 每天实际能投入多少小时？周末是否有整块时间？ | 决定三阶段的时间切分，以及能不能承受"环境配置"这类前置成本 | ⏳ 未知 |
| **Q11** | 腾讯云能否绑定支付方式（微信/支付宝）？ | 免费额度到期续期、超出额度的处理都需要；不能绑则免费版到期即停 | ⏳ 未知 |
| **Q12** | 你的电脑会长期开机吗？（比如上课期间是否常开） | 决定"自建服务"能不能作为断网/额度耗尽时的兜底 | ⏳ 未知 |

### 0.2 临时假设

| 编号 | 临时假设 | 影响的章节 | 若被推翻 |
|------|---------|-----------|---------|
| T1 | 将新开通 CloudBase 环境，**数据库类型选 PostgreSQL**（上海地域） | 3、5.5、11.3 | 若选文档型：5.5 改为集合结构，11.3 脚本调整 |
| T2 | 部署先用**控制台 + CLI** 手动完成，不做 CI 自动构建 | 4、10 | 若上 Git 自动构建：第 10 章需加构建配置 |
| T3 | 前端 Day 6–21 用**原生 HTML/CSS/JS**，暂不引入 Vite/React | 3.3、11.2 | 若必须上 React：按 11.2 迁移清单执行 |
| T4 | 收藏本期存**浏览器本地**，不落库、不登录（沿用 PRD v2） | 5.4 | 若改为云端：启用 5.5 表 + 6.3 接口 |
| T5 | 4 个平台**至少能拿到 2 个**可用来源，其余按降级处理 | 8 | 若全部拿不到：需换数据源（可能要密钥与费用） |
| T6 | 免费额度够本项目 28 天使用 | 2.5、10.8 | 若不够：评估套餐或降低刷新频率 |
| T7 | 无需自定义域名，先用平台默认域名上线 | 8.5、10.7 | 若需自定义域名：预留备案时间（按周计） |
| T8 | 页面与接口**不同域**，需显式处理跨域（CORS） | 6.4、8.2 | 若配置同域自定义域名：CORS 一条可删 |
| **T9** | 你能跑通 `npm install -g`（有权限） | 10.1、10.3 | 若不能：部署改为控制台手工上传 + 在线编辑函数 |
| **T10** | 每天可投入 1–2 小时，周末可能更多 | 3.2 | 若时间更少：三阶段合并为两阶段，砍掉交互打磨的迭代次数 |
| **T11** | 腾讯云账号可实名且可绑支付方式 | 2.5、10.8 | 若不能：路线降级为方案 A（见 2.7 节降级条件） |
| **T12** | 电脑不保证长期开机 | 2.4、2.6 | 若能长期开机：可把"自建小服务"当兜底方案 |

### 0.3 全文标注规则

- ✅ **已核实**：有官方来源（第 16 章列出）。
- 🔶 **临时假设**：依赖 0.2 节假设才成立。
- ⏳ **待核实**：需要你确认，或在 Day 8 前确认。

### 0.4 我的已知限制（电脑 / 时间 / 账号）及对选型的影响

**电脑限制**

| 已知情况 | 影响 | 应对 |
|---------|------|------|
| Windows 系统，**用户名含中文**（`C:\Users\唯我独尊`） | 已在 Day 2 实际踩坑：git 的 `-i` 参数带中文路径会直接段错误；部分工具对非 ASCII 路径支持差 | 命令与配置一律用 ASCII 路径；`git core.sshCommand` 指向 Windows 自带 OpenSSH（已配好）；项目路径保持桌面短路径 |
| Node v22.22.2、Git 2.55.0、Windows 自带 OpenSSH **都已可用** | 开发环境**零安装成本** | 不需要装任何本地工具即可开始写代码 |
| 但 CloudBase CLI 属于 npm 全局包，**是否可安装未知** | 影响部署方式（命令行 vs 控制台） | Q9；若不可安装 → 走控制台手工上传（步骤见 10.2、10.3 的替代路径） |
| 家宽到 GitHub 的 **22 端口被拦** | 影响推送 | 已改用 `ssh.github.com:443`（Day 2 配好，推送正常） |

**时间限制**

| 已知情况 | 影响 | 应对 |
|---------|------|------|
| 28 天课程制，每天一个任务清单；**Day 1–5 已用掉**（含 Day 2 一天花在 GitHub/SSH 排错上） | 不能承受"推倒重来"，也来不及学一堆新工具 | ① 技术栈尽量不新增（原生 JS + 云函数）；② **先做能跑的，再谈优化**（AGENTS.md 第三节已有约定） |
| 部署如果拖到 Day 28 才第一次做，风险极高 | 部署是"一次性未知数"，最容易最后一天翻车 | **把首次部署提前到 Day 7**（阶段 1 只传静态页，见 10.2），让"上线"从第一天起就是已知数 |
| 没有余力学 CI/自动化测试 | 质量靠人工逐条验收 | 明确列入"本期不做"（第 14 章），用 PRD 的 B1–B12 人工点检代替 |

**账号限制**

| 已知情况 | 影响 | 应对 |
|---------|------|------|
| GitHub 账号 `Acknowledge-zyh` 已就绪，私有仓库免费 | 版本管理无阻碍 | 继续沿用，提交署名已用隐私邮箱 |
| **不建议把真实邮箱暴露在提交记录里**（已用 GitHub 隐私邮箱） | — | 已完成，无需改动 |
| 腾讯云账号：**是否有、能否实名、能否绑支付方式均未知** | 这是方案 B 的**唯一硬门槛** | Q1 / Q11；若无法满足，按 2.7 节的降级条件退回方案 A |
| 一切账号、密钥不得写入文件或代码 | AGENTS.md 第五条既定规则 | 密钥只放本地 `.env`（已忽略）与云端环境变量（第 9 章） |

---

## 1. 一句话说清数据从哪来、到哪去

> **数据从 4 个平台的公开热榜来源来，经云函数定时取回、统一格式并缓存，前端页面一次请求全部取回、渲染成一屏四列；用户的收藏与备注只写进浏览器本地存储，不回传服务器。**

两条链分开看：

- **读链（热搜）**：平台 → 云函数（取回 + 统一 + 缓存）→ 前端页面 → 用户的眼睛
- **写链（收藏）**：用户点击 → 浏览器本地存储 → 下次打开还在（不出这台设备）

> 二期（若启用云端收藏）写链变成：用户点击 → 云函数 → PostgreSQL → 换设备也在。

---

## 2. 方案 A 与方案 B：四维比较与推荐

### 2.1 两套候选方案是什么

| 方案 | 一句话 | 四件套 |
|------|--------|--------|
| **方案 A：纯前端 + 浏览器本地** | 只有一个网页，不要后端；热搜数据用前端去要（或退化为静态样本），收藏与备注存在浏览器里 | 前端：原生 HTML/CSS/JS｜后端：无｜数据：localStorage｜部署：静态托管（CloudBase 或 GitHub Pages） |
| **方案 B：CloudBase 云开发全托管**（示范路线） | 网页托管在云上，数据获取交给云函数，需要存数据时用云数据库 | 前端：原生起步（可升级 React/Vite）｜后端：CloudBase Node.js 云函数｜数据：本期 localStorage + 二期 PostgreSQL｜部署：CloudBase 静态网站托管（自动 HTTPS + CDN） |

> **曾经考虑、现已淘汰的第三套**：原生页 + 自建 Node/Express 服务（Day 5 上午 v1 初稿）。淘汰原因一句话：**需要一台一直开着的机器**，关机即下线，达不成 PRD B12「链接可长期访问」；且换电脑要重装环境，成本落在你身上。它仍保留一个用途：**本地开发期快速调试**（见 11.1）。

### 2.2 四维比较总表

| 维度 | 方案 A 纯前端 + 本地 | 方案 B 云开发全托管 | 谁赢 |
|------|-------------------|-------------------|------|
| **学习成本** | 几乎为零：只用手上已有的 HTML/CSS/JS + localStorage | 中：要新增 5 个概念（环境、环境 ID、CLI、云函数入口写法、HTTP 访问服务） | **A** |
| **线上持久化** | 弱：热搜数据无服务端；收藏只在**这一台设备的这个浏览器**里 | 强：热搜由云函数服务端取回并缓存；收藏二期可落 PostgreSQL 跨设备 | **B（差距最大）** |
| **费用** | 0 元 | 0 元可起步（免费体验版），但**到期未续会停服**；启用 PG 会消耗额度 | **A（但差距小）** |
| **排错难度** | 出错面窄，但**错在"自己无法解决"的地方**（跨域/反爬），报错吓人 | 出错面宽（配置类），但**错在"有日志、可回滚、能查"的地方** | **B（长期视角）** |
| **能否满足 PRD 硬指标** | ❌ B1（40 条真实数据）基本无望 | ✅ B1 + B12 都能满足 | **B** |

### 2.3 维度一：学习成本

| 比较项 | 方案 A | 方案 B |
|--------|-------|-------|
| 需要新学的概念数 | 0–1 个（若用第三方开放接口，需懂一点点 CORS） | 5 个：环境 / 环境 ID / CLI 部署 / 云函数入口写法 / HTTP 访问服务 ✅ |
| 第一个"能在浏览器打开"的版本 | 当天就有（Day 6 就能做） | 同上（骨架阶段两者没差别，都是静态页）⚠️ 关键：**差别只在"接数据"那一步才出现** |
| 第一次部署要多久 | 10 分钟（传文件） | 30–60 分钟（建环境 + 传静态页 + 配函数）🔶 |
| 文档友好度 | 网上教程极多 | 官方文档齐全，但偏工程口吻，遇到报错需要 AI 帮忙翻译 🔶 |
| 对零基础的净负担 | 最低 | 中等，**但这份负担只付一次**（后面加功能不再重复付） |

**结论**：学习成本上 A 明显更省。但要注意：A 省下的学习成本，换来的是 2.7 节里"达不成 B1"的死结——**省下的时间会在第 2 周以加倍的挫败还回来**。

### 2.4 维度二：线上持久化（最关键的差异）

先明确"线上持久化"问的是三件事：**数据存在哪、关掉电脑还在不在、换设备还有没有。**

| 问题 | 方案 A | 方案 B |
|------|-------|-------|
| 热搜数据存在哪 | 前端每次打开时去要；要不到就只剩静态样本（数据是"死"的） | 云函数服务端取回后缓存 10 分钟，**服务端常驻**（实例回收需重建缓存）🔶 |
| 关掉我的电脑后，网站还在吗 | ✅ 在（静态页托管在云上） | ✅ 在（同样托管在云上） |
| 关掉电脑后，热搜还能更新吗 | ❌ **不能**——没有服务端去取数 | ✅ 能——取数发生在云端，与你电脑无关 |
| 收藏与备注关掉浏览器还在吗 | ✅ 在（浏览器本地） | ✅ 在（本期同样存本地） |
| 收藏换设备/换浏览器还在吗 | ❌ 没有 | ⏸️ 本期也没有，**二期启用 PostgreSQL 后才有** |
| 清浏览器缓存会丢吗 | ⚠️ 会丢（PRD 已写提示文案） | ⚠️ 本期同样会丢（二期落库后不丢） |
| 服务端有"仓库"吗 | 完全没有 | 有：先云函数缓存，二期上 PostgreSQL ✅ |

**结论**：**这是两套方案差距最大的维度。** 方案 A 根本没有"服务端持久化"这个能力，任何需要"服务器记住点什么"的功能它都做不到；方案 B 现在是"半个持久化"（热搜在云端、收藏在本地），二期补上 PG 就是完整持久化。**如果本期只做一个能看的静态页，A 够用；但 PRD 明确要求 40 条真实数据且长期可访问——那 A 就不够了。**

### 2.5 维度三：费用

| 比较项 | 方案 A | 方案 B |
|--------|-------|-------|
| 起步成本 | 0 元 | 0 元（免费体验版）✅ |
| 具体免费口径 | 静态托管在免费额度内；或直接用 GitHub Pages 免费 | 官方口径：每个云开发账号 **1 个免费环境、3,000 资源点/月，单次续期 6 个月**✅；额度用尽或到期未续，环境可能停服乃至销毁 ⏳ |
| 什么时候开始花钱 | 基本不会 | 流量/调用量超出免费额度，或需要更长有效期时；个人版为按月计费套餐 ⏳ |
| 隐形费用（真正的坑） | 无 | **"忘了续期"** —— 免费环境不是永久免费的，到期提醒要自己盯 |
| PostgreSQL 的费用影响 | 不涉及 | 本期不启用；二期若启用会消耗额度 ⏳ |

**结论**：费用上两者都能 0 元起步，差距很小。**方案 B 的真实成本不是钱，是"续期这件事要记在日历上"**（对应动作见 10.8）。

### 2.6 维度四：排错难度

| 比较子项 | 方案 A | 方案 B |
|---------|-------|-------|
| 可能出错的环节数 | 少（就一个页面） | 多（环境 / CLI / 云函数 / 托管 四处） |
| 典型错误长什么样 | 页面空白，控制台一行红字 `blocked by CORS policy` / `403` | 部署时提示环境 ID 不对、函数上传失败、接口 404 |
| 这类错误你能不能自己修 | ❌ **不能**——跨域与反爬不是你的代码问题，改代码没用 | ✅ 多数能——看日志、看是否漏配 HTTP 访问、看环境 ID 拼错没 |
| 有没有"官方给的工具"帮你定位 | 基本没有 | 有：云函数日志、静态托管版本记录与回滚、CLI 的报错信息（比浏览器报错更具体） |
| 失败时的影响范围 | 整页空白（一个环节挂了全挂） | **可局部失败**：某平台失败只影响那一列（这正是 PRD B10 的设计，也让排错天然分段） |
| 能不能回滚 | 能（重新上传旧文件） | 能，且**平台自带版本回滚** ✅ |

**结论**：方案 A 的错误"少但无解"，方案 B 的错误"多但有解"。新手阶段容易误判为 A 更简单，**但真正卡死人的是 A 那种无解错误**。

### 2.7 综合结论：推荐**方案 B**

**推荐方案 B（CloudBase 云开发全托管），并在 Day 6–7 用它的"静态托管"部分先上线骨架。**

理由按重要性排序：

1. **只有 B 能满足 PRD 的两条硬指标**：B1（40 条真实数据）需要服务端出网绕开跨域与反爬；B12（链接可长期访问）需要托管平台保证。A 两条都做不到，自建服务只能做到 B1。
2. **线上持久化是"以后能不能长大"的分水岭**：B 已经有一个服务端在云端跑着，二期加登录、加云端收藏、加归档都只是"往同一个环境里加东西"；A 要长大必须先补一个后端，等于推倒重来。
3. **排错难度按"长期"算 B 反而更低**：B 的错误有日志、有回滚、可局部降级，而 A 的错误（跨域/反爬）对零基础是无解死路。
4. **费用不是决定因素**：两者都能 0 元起步，B 只需多记一件"到期续期"的事。

**"为什么不先用 A 以后再换 B"** —— 因为你换的时候要付两遍成本：前端代码要改（接口地址、数据格式）、数据要迁（本地收藏挪上云）、习惯要改（从"直接开文件"变成"部署后看线上"）。**同样的时间，不如一开始就走 B 的分阶段路：Day 6–7 阶段 1 的形态其实和 A 一样简单（纯静态页），但方向是对的。**

**降级条件（必须记住）**：若 Q1 / Q11 的答案是"没有腾讯云账号 / 无法实名 / 无法绑支付方式"，**就退回方案 A**：用静态样本 + 前端直连可用的开放接口，收藏仍存本地，在文档与打卡里明确记录"数据为样本/待核实"。这不是失败，是 28 天课程里的合理取舍。

### 2.8 两套方案"会在哪里卡住"（提前拆弹）

| 方案 | 最可能卡住的点 | 拆弹办法 |
|------|--------------|---------|
| A | 页面打开一片空白，控制台报 CORS / 403 | 承认这不是代码问题，直接换 B；或降级为"静态样本数据"并在文档标注 |
| A | 想给朋友看，但数据都是旧的样本 | 同上，或改用可用的第三方开放接口（要评估稳定性与合规） |
| B | 卡在"环境怎么建、环境 ID 填哪、部署命令报什么错" | **一次只加一层**：Day 7 只做静态托管 → Day 8 加云函数 → 二期才碰数据库（见 3.2、第 10 章） |
| B | 免费额度到期，网站突然打不开 | 开通当天就在日历里加"到期前 2 周续期"提醒（10.8） |

---

## 3. 推荐路线与取舍

### 3.1 结论

**默认路线 = 方案 B，分三阶段启用，每阶段单独验收。**

一句话：**只用方案 B 能同时拿到 PRD 的两条硬指标**——B1（真实数据）需要服务端出网，B12（长期可访问）需要托管平台保证，而云开发一次性解决这两件事，还免去运维。

### 3.2 三阶段落地表

| 阶段 | 时间 | 前端 | 后端 | 数据库 | 部署 | 对应验收 |
|------|------|------|------|--------|------|---------|
| **阶段 1：骨架上线** | Day 6–Day 7 | 原生 HTML/CSS/JS，数据用本地样本 `data/hot.json` | 不接 | 不用 | **只做静态托管**，先把"网页能打开、链接能访问"跑通（第 10.2 节） | A1、A2（Day 7） |
| **阶段 2：真实数据** | Day 8–Day 14 | 原生页，改为请求 `/api/hot` | **接 Node 云函数**：取回 4 平台数据、统一字段、缓存、定时刷新 | 不用 | 云函数 + 静态托管（第 10.3 节） | B1、B2、B8、B10 |
| **阶段 3：体验与上线** | Day 15–Day 28 | 交互打磨（详情弹层、收藏抽屉、移动端） | 云函数保持只读 + 缓存 | **默认仍不用**；要做云端收藏再启用 PG | 全量部署 + 版本回滚（第 10.4 节） | B3–B7、B9、B11、B12 |

### 3.3 三个关键取舍

**取舍一：前端为什么本期不上 React/Vite？**
- 本项目只有**一个页面 + 两个浮层**（PRD 第 5.1 节）。React/Vite 解决的是"页面多、状态复杂、多人协作"的问题，本项目没有这些痛点。
- 上构建工具会多出三件事：装依赖、跑构建、把「源码目录」和「托管目录」分开（部署要传 `dist/` 而不是源码）。对零基础，每一件都是一处新坑，而 Q10 显示你的时间并不宽裕。
- 因此本期不上，但**留好升级路径**（11.2 节迁移清单）。若 Q5 答案是"老师强制"，按该清单在 Day 12 前后迁移——代价是前端重写一遍，**云函数与接口契约完全不用动**（这正是先把契约固定的价值）。

**取舍二：数据库为什么本期不启用，但环境要选 PostgreSQL 类型？**
- PRD 明确：没有登录、收藏存本地。**没有账号体系，数据库存什么都无从归属**——表建了也是空的。
- 但环境一旦创建，数据库类型就定了，**文档型与 PostgreSQL 不可在同一环境混用**✅。所以开通环境时就要替二期做决定：选 PostgreSQL，二期可直接加表，不用重建环境。
- 代价：数据库类型的选择会影响地域与计费口径 ⏳（见 Q2 / Q6）。

**取舍三：为什么用云函数，而不是自建 Express？**
- 两者**接口契约完全一样**（都是 `GET /api/hot`），区别只在"跑在哪"。
- 自建 Express 需要你保证电脑常开、自己配 HTTPS 与域名（T12 假设你不保证常开）；云函数把这三件事交给平台（自动扩缩容、自动 HTTPS、免运维）✅。
- 保留自建 Express 的**唯一场景**：本地开发期快速调试（11.1）。

### 3.4 与 v1 初稿（提交 b042db9）的差异说明

| 项目 | v1 初稿 | v3 现稿 | 为什么改 |
|------|---------|---------|---------|
| 后端 | 自建 Node + Express | CloudBase Node 云函数（本地仍可临时用 Express 调试） | 免运维 + 自动 HTTPS + 支撑 B12，且不依赖你电脑常开 |
| 部署 | "部署平台待 Day 22 选定" | CloudBase 静态托管，**Day 7 就首部署** | 把"能上线"提前验证，避免 Day 28 才发现部署卡住 |
| 数据库 | 本期不用（未提方案） | 本期不用，但**环境选 PostgreSQL 类型**为二期预留 | 数据库类型事后不可混用 ✅ |
| 前端 | 原生（结论相同） | 原生 + 明确升级路径与迁移清单 | 补上"以后怎么改" |
| 比较方式 | 三方案、按通用维度 | **A/B 两套，按学习成本/线上持久化/费用/排错难度四维** | 本次要求，四维更贴近"零基础能不能扛住" |
| 新增章节 | — | 项目结构、数据对象与字段、API 列表、数据流、错误处理、环境变量、**部署**、迁移注意事项、已知限制 | 本次要求 |

---

## 4. 项目结构

> 下面的文件**绝大多数还不存在**（标 ⏳），本章是"待创建清单"，不是已建文件清单。

```
VibeCoding/                         ← 仓库根目录，也是 Git 仓库
├─ .gitignore                       ✅ 已存在（含 .env、密钥、*.db、打卡/）
├─ AGENTS.md                        ✅ Day 1 规则文件
├─ research.md                      ✅ Day 3 产品研究
├─ PRD.md                           ✅ Day 4 产品需求（v2）
├─ TECH_DESIGN.md                   ✅ Day 5 技术方案（本文件）
├─ index.html                       ⏳ 主页面（Day 6 起替代 Day 2 的占位页）
├─ styles.css                       ⏳ 样式（Day 6）
├─ app.js                           ⏳ 前端交互：渲染 / 详情弹层 / 收藏 / 刷新（Day 6）
├─ data/
│   └─ hot.json                     ⏳ 本地样本数据（阶段 1 用；阶段 2 后作兜底）
├─ cloudfunctions/                  ⏳ 阶段 2 才创建（云函数根目录）
│   └─ hot/
│       ├─ index.js                 ⏳ 入口：路由 /api/hot 与 /api/health
│       ├─ sources.js               ⏳ 4 个平台的数据来源适配（**换源只改这个文件**）
│       ├─ normalize.js             ⏳ 统一字段（映射成 HotItem）
│       └─ package.json             ⏳ 依赖声明
├─ cloudbaserc.json                 ⏳ CloudBase 部署配置（环境 ID / 函数根目录 / 托管目录）
├─ .env                             ✅ 已存在但**不入库**（敏感配置）
└─ 打卡/                            ✅ 已存在但**不入库**（每日截图）
```

**入库 vs 不入库**

| 内容 | 入库？ | 说明 |
|------|-------|------|
| 源码与文档（含 `cloudbaserc.json`、`data/hot.json`） | ✅ | 要版本记录；样本数据里只有公开标题与链接 |
| `.env`、密钥文件 | ❌ | `.gitignore` 已覆盖，Day 2 已实测拦截成功 |
| `node_modules/`、构建产物 `dist/` | ❌ | 可重新生成 |
| `打卡/` | ❌ | 本地留存 |

---

## 5. 数据对象与字段

**一句话原则**：前端只认一种结构，后端负责把 4 个平台的不同格式"翻译"成这一种。**形状定下就不许随便改**（前端、云函数、缓存、将来的数据库表都依赖它）。

### 5.1 数据对象 `HotItem`（热搜条目）

严格对齐 PRD 第 6.1 节：

| 字段 | 类型 | 说明 | 必填 |
|------|------|------|------|
| platform | string | 平台名，取值 `微博` / `百度` / `知乎` / `抖音` | 是 |
| rank | number | 排名 1–10 | 是 |
| title | string | 标题（前端负责截断显示，后端不截断） | 是 |
| url | string | 原文链接（http/https 开头） | 是 |
| heat | string \| null | 热度值原样保留（有则显示，无则 `null`，**不显示 0**） | 否 |
| time | string \| null | 上榜/更新时间（有则显示） | 否 |

### 5.2 数据对象 `HotResponse`（`GET /api/hot` 的返回体）

```json
{
  "updated_at": "2026-09-20T10:40:00+08:00",
  "platforms": [
    { "name": "微博", "ok": true,  "message": null,                      "updated_at": "2026-09-20T10:40:00+08:00" },
    { "name": "百度", "ok": true,  "message": null,                      "updated_at": "2026-09-20T10:40:00+08:00" },
    { "name": "知乎", "ok": false, "message": "暂时无法获取，稍后自动重试", "updated_at": null },
    { "name": "抖音", "ok": true,  "message": null,                      "updated_at": "2026-09-20T10:40:00+08:00" }
  ],
  "items": [
    { "platform": "微博", "rank": 1, "title": "示例标题", "url": "https://example.com/a", "heat": "1234567", "time": "10:30" }
  ]
}
```

| 设计点 | 说明 |
|--------|------|
| `platforms[].ok` | **降级开关**：前端按它决定哪列显示列表、哪列显示"暂时无法获取"（PRD 第 7 节、验收 B10） |
| `items[]` | 只放成功的条目，**最多 40 条**（4×10）；某平台失败不影响其余 |
| `updated_at` | 整份数据的时间戳，顶栏"更新于 HH:MM"取它 |

### 5.3 数据对象 `HotCache`（云函数内缓存）

| 字段 | 说明 |
|------|------|
| items | 上次成功取回的 HotItem 数组 |
| platforms | 上次各平台成功状态 |
| fetched_at | 上次成功取回的时间 |
| TTL | `CACHE_TTL_MINUTES`，默认 **10 分钟**（对齐 PRD F4） |

| 行为 | 设计 | 理由 |
|------|------|------|
| 缓存位置 | 云函数内共享变量（实例存活期间有效）🔶 | 零成本、零依赖；实例回收后重拉一次不影响体验 |
| 过期 | 先返回旧数据，再后台更新（"先给人看，再去进货"） | 保证 B2「首屏 ≤2 秒」永远成立 |
| 取数失败 | 仍返回旧缓存 + 旧时间戳 | 对齐 PRD「绝不显示空白页」 |

### 5.4 数据对象 `FavoriteItem`（本期：浏览器 localStorage）

严格对齐 PRD 第 6.2 节：

| 字段 | 类型 | 说明 |
|------|------|------|
| id | string | 前端生成（如 `时间戳 + 平台 + 排名`），用于更新与删除 |
| platform | string | 收藏时从原条目带入 |
| title | string | 收藏时从原条目带入 |
| url | string | 收藏时从原条目带入 |
| note | string | 用户备注，**≤50 字**，可空、可改、可删 |
| saved_at | string | 收藏时间（ISO 格式），列表按此倒序 |

存储约定：整体存成一个数组，键名带版本号（如 `hot_favorites_v1`），便于将来结构升级时识别旧数据。**不含任何身份信息，不回传服务器。**

### 5.5 二期数据库表（CloudBase PostgreSQL，本期不建表）

```sql
-- 二期启用；本期不执行
create table if not exists favorites (
  id         bigserial primary key,
  user_id    text        not null,          -- 二期登录后填用户标识；本期无登录，字段先留
  platform   text        not null,
  title      text        not null,
  url        text        not null,
  note       varchar(50),
  saved_at   timestamptz not null default now(),
  unique (user_id, platform, title)          -- 同一用户同一条热搜只留一条，天然防重复
);

create index if not exists idx_favorites_user_saved
  on favorites (user_id, saved_at desc);     -- 对齐"按收藏时间倒序"
```

| 设计点 | 说明 |
|--------|------|
| 唯一键用 `platform + title` 而非 `url` | 原文链接常带跟踪参数，同一条热搜的 url 可能变；标题 + 平台更稳定 |
| 行级权限（RLS） | CloudBase PostgreSQL 自带 ✅：二期"只能读写自己那几行"用 SQL 策略表达，不必自己写鉴权 |
| 接口自动化 | 建表后可自动生成 RESTful 接口（CRUD 即用）✅，二期接口可能不用手写云函数 |
| 迁移 | 见 11.3（本地收藏怎么搬上云，不能让用户白收藏） |

---

## 6. API 列表

### 6.1 本期唯一必需接口

| 项目 | 内容 |
|------|------|
| 接口 | `GET /api/hot` |
| 谁调用 | 前端页面（打开时、点刷新时） |
| 作用 | 返回 4 个平台 Top 10 聚合数据 + 各平台成功状态 |
| 请求参数 | 无（第一阶段不接受参数，减少攻击面） |
| 成功响应 | `200` + HotResponse（5.2） |
| 失败响应 | `500` + `{ "error": { "code": "UPSTREAM_ALL_FAILED", "message": "暂时无法获取数据" } }` |
| 缓存 | 服务端缓存 10 分钟；命中即返回 |
| 形态 | CloudBase Node.js 云函数，经平台 HTTP 访问地址对外 🔶 |

> 🔶 前端要访问的完整地址（形如 `https://<环境ID>.service.tcloudbase.com/api/hot`）**以控制台实际显示为准** ⏳。建议写成前端一处常量 `const API_BASE = "..."`，换环境只改一行。

### 6.2 运维自检接口

| 项目 | 内容 |
|------|------|
| 接口 | `GET /api/health` |
| 作用 | 返回 `{"ok":true,"time":"...","cache_age_seconds":123}`，一眼看出服务活着、缓存多旧 |
| 为什么值得写 | 页面空白时先敲这个地址，就能判断"是服务挂了还是前端写错了"——**排错提速最划算的一个接口**（第 10.3 节会用它） |

### 6.3 二期接口（本期不实现，仅登记）

| 接口 | 作用 | 备注 |
|------|------|------|
| `GET /api/favorites` | 取当前用户收藏列表 | 需登录（二期） |
| `POST /api/favorites` | 新增收藏（含备注） | 命中唯一约束时返回"已存在" |
| `PATCH /api/favorites/:id` | 修改备注 | 备注 >50 字返回 400 |
| `DELETE /api/favorites/:id` | 取消收藏 | — |
| `GET /api/hot?refresh=1` | 强制立即刷新（绕过缓存） | ⏳ 需限流（如每 IP 每分钟 1 次），否则可能被上游封 |

### 6.4 接口约定（通用）

| 约定 | 内容 |
|------|------|
| 统一包裹 | 成功直接返回数据对象；失败返回 `{ "error": { "code", "message" } }` |
| 错误码 | `UPSTREAM_TIMEOUT`、`UPSTREAM_ALL_FAILED`、`BAD_REQUEST`、`RATE_LIMITED`、`INTERNAL` |
| 跨域（CORS） | 🔶 页面与接口不同域，云函数需返回允许来源的响应头；**上线前必须实测一次** |
| 超时 | 上游读取超时 `FETCH_TIMEOUT_MS`（默认 8000ms），超时即降级，不许挂住整个接口 |
| 版本 | 本期路径不带版本号；将来改结构用 `/api/v2/hot`，不要原地改（保护已上线的页面） |

---

## 7. 前后端数据流

```
   数据从哪来 →  ┌───────────────────────────────────────────────┐
                │  微博    百度    知乎    抖音                  │
                │  （各平台公开热榜来源，格式各不相同）           │
                └───────────────────┬───────────────────────────┘
                                    │ ① 定时取回（间隔 ≥10 分钟）
                                    ▼
                ┌───────────────────────────────────────────────┐
                │  CloudBase Node.js 云函数（服务端，用户看不见） │
                │  ② 统一字段：platform/rank/title/url/heat/time │
                │  ③ 缓存 10 分钟（过期先返旧、后台再更新）       │
                │  ④ 按平台分别记录成功/失败状态                 │
                └───────────────────┬───────────────────────────┘
                                    │ GET /api/hot（一次请求取全量）
                                    ▼
                ┌───────────────────────────────────────────────┐
                │  前端页面（托管在 CloudBase 静态托管）          │
                │  ⑤ 渲染一屏四列；失败的板块显示降级提示         │
                │  ⑥ 详情弹层 / 刷新 / 收藏抽屉                  │
                └───────────┬─────────────────────┬─────────────┘
                            │                     │
           用户的眼睛 ←──────┘                     │ ⑦ 点收藏 / 写备注
           （看榜单、点原文跳原平台）                ▼
                                    ┌───────────────────────────┐
                                    │  浏览器本地存储 localStorage │
                                    │  收藏 + 备注（只在本机）    │
                                    └──────────┬────────────────┘
                                               │ 下次打开仍在
                                               ▼
                                       数据到哪去 → 用户自己的屏幕
```

### 7.1 数据流九步

| 步骤 | 发生什么 | 数据形态 | 谁负责 |
|------|---------|---------|--------|
| ① 取回 | 云函数按 ≥10 分钟节奏向各平台来源要榜单 | 原始数据（各家格式不同） | 云函数 |
| ② 统一 | 映射成 `HotItem` 一套字段 | 统一结构（5.1） | 云函数 |
| ③ 缓存 | 统一后的 40 条暂存 10 分钟 | `HotCache`（5.3） | 云函数 |
| ④ 标记状态 | 记录哪几家成功、哪家失败及原因 | `platforms[]` | 云函数 |
| ⑤ 传输 | 前端请求一次 `/api/hot`，一次拿全 | `HotResponse`（5.2） | 接口 |
| ⑥ 渲染 | 四列并排；失败板块显示提示 | 用户可见页面 | 前端 |
| ⑦ 交互 | 点条目出详情弹层、点刷新重新请求 | 页面状态 | 前端 |
| ⑧ 写入 | 收藏与备注写入浏览器本地存储 | `FavoriteItem`（5.4） | 前端 |
| ⑨ 持久 | 下次打开从本地读回收藏 | 同上 | 前端 |

### 7.2 三条"绝不"

- 热搜数据**绝不**由前端直接向平台索取（跨域 + 反爬）——一律经云函数中转。
- 收藏数据**绝不**回传服务器（本期无账号体系，不收集任何用户数据）。
- 密钥**绝不**出现在前端代码或前端环境变量里（前端打包后一切可见）。

---

## 8. 错误处理

**总原则：单点失败不许影响整页。** 一个平台挂了其余三个照常；数据晚到，显示旧数据也不许白屏。

### 8.1 数据源层（云函数内部）

| 情况 | 处理 | 用户看到 |
|------|------|---------|
| 某平台请求超时（> 8s） | 记失败，放弃该平台，继续处理其余 | 该板块"暂时无法获取，稍后自动重试" |
| 某平台返回 403/404/429 | 同上；429 则**拉长该平台刷新间隔**（避免越封越死） | 同上 |
| 页面结构变了导致解析为空 | 视为失败，**绝不返回空板块冒充成功** | 同上 |
| 4 个平台全部失败 | 返回旧缓存 + 旧时间戳；无缓存则返回 500 | 有缓存：页面正常但时间戳是旧的；无缓存：整页提示 |
| 字段缺失（如 heat 为空） | 缺失字段置 `null`，其余照常 | 该条不显示热度，不显示"0" |

### 8.2 云函数与接口层

| 情况 | 处理 |
|------|------|
| 冷启动慢（首次调用需唤醒） | 前端显示骨架占位，不显示空白 🔶 视实测决定是否预热 |
| 跨域被拦（CORS） | 云函数返回允许来源响应头；上线前必须实测 |
| 被刷（同一 IP 高频调用） | 返回 `429 RATE_LIMITED`；前端提示"操作太频繁，稍后再试" |
| 未捕获异常 | 统一兜底：记日志 + 返回 `500 INTERNAL`，不外泄内部堆栈 |

### 8.3 前端层

| 情况 | 处理 | 对应 PRD |
|------|-----|---------|
| 请求失败或超时 | 显示页面上次数据 + 原时间戳，并提供"重试" | 第 7 节 |
| 某板块降级 | 只该板块显示提示，其余完整可用 | B10 |
| 本地存储不可用（隐私模式） | 点收藏时提示"当前浏览器环境不支持保存收藏"，浏览不受影响 | 第 7 节 |
| 收藏条目已下榜、原文 404 | 收藏记录照常显示，不处理 | 第 7 节 |
| 手机竖屏 | 板块纵向单列、无横向滚动；弹层全屏 | B9 |

### 8.4 三层排查法（人也会出错）

| 症状 | 第一步查哪里 | 判断依据 |
|------|-------------|---------|
| 页面整片空白 | 敲 `GET /api/health` | 返回 `ok:true` → 问题在前端；返回错误 → 问题在云函数 |
| 某板块一直失败 | 云函数日志里该平台的错误 | 403/429 是反爬或限流；解析空是页面结构变了 |
| 部署后还是旧页面 | 静态托管版本记录 | 有版本与回滚能力 ✅，可回滚验证 |

### 8.5 部署与域名类错误

见第 10.6 节的「部署失败对照表」（症状 / 原因 / 第一步查哪）。

---

## 9. 环境变量与密钥管理

### 9.1 铁律三条

1. **前端不放任何密钥**：前端代码与前端环境变量（如 Vite 的 `VITE_` 前缀变量）打包后会全部暴露。前端只需要**一个非敏感的接口地址**。
2. **敏感值只存两处**：① 本地开发时存项目根 `.env`（**已确认被 `.gitignore` 覆盖，不会入库**）；② 线上存**云函数的云端环境变量**（控制台配置，不落仓库）。
3. **`.env` 一旦误提交，改值也晚了**——要立刻在源头作废重发（Git 历史删不干净）。

### 9.2 变量清单

| 变量名 | 用在哪 | 示例 | 敏感？ | 存哪 |
|--------|-------|------|-------|------|
| `TCB_ENV_ID` | CLI 部署、`cloudbaserc.json` | `env-xxxxxxxx` | 否（配置项） | `cloudbaserc.json`（可入库） |
| `CACHE_TTL_MINUTES` | 云函数 | `10` | 否 | 云端环境变量 |
| `FETCH_TIMEOUT_MS` | 云函数 | `8000` | 否 | 云端环境变量 |
| `API_BASE` | 前端 | `https://<环境ID>.service.tcloudbase.com` | 否 | 前端常量（**不是密钥**）🔶 |
| 各平台来源地址/接口名 | 云函数 `sources.js` | 按平台而定 | 否 | 代码内常量 |
| **第三方数据 API Key**（若用） | 云函数 | `sk-xxxx` | **是** | 云端环境变量（**绝不入库、绝不进前端**） |
| **`DATABASE_URL`**（二期启用 PG） | 二期云函数 | `postgresql://user:pwd@host:5432/db` | **是** | 云端环境变量 |
| `PORT` | 本地调试用 Express（仅阶段 2 前） | `3000` | 否 | 本地 `.env` |

### 9.3 `.gitignore` 现状（已实测通过）

已覆盖 `.env`、`.env.*`、`*.pem`、`*.key`、`secrets*.json`、`credentials*.json`、`*.db`、`node_modules/`、`打卡/`。Day 2 用三个假文件（`.env`、`test.db`、`secrets.json`）实测全部被拦截 ✅。

---

## 10. 部署

> 本章解决一件事：**让"上线"从 Day 7 起就是已知数，而不是 Day 28 的未知数。**

### 10.1 部署前检查清单

- [ ] CloudBase 环境已开通，**环境 ID 已记下**（形如 `env-xxxx`）
- [ ] 环境创建时已按需选择数据库类型（本期用不到，但**一旦创建不可改**，见 Q2）
- [ ] CloudBase CLI 可用并已登录（T9 假设；若装不了，走控制台手工上传的替代路径）
- [ ] `cloudbaserc.json` 里 `envId`、函数根目录、托管目录三项与实际一致
- [ ] 前端 `API_BASE` 指向当前环境的接口地址（阶段 2 才需要）
- [ ] `git status` 干净：要部署的版本已提交（**出问题才能回退**）
- [ ] `.env` 未被提交（`.gitignore` 已覆盖 ✅）

### 10.2 阶段 1：只上静态页（目标：拿到一个能打开的网址）

| 步骤 | 操作 | 成功现象 |
|------|------|---------|
| 1 | 部署静态资源（CLI 的托管部署命令，或控制台上传静态目录） | 返回一个访问地址 |
| 2 | 浏览器打开该地址 | 页面出现四列占位、顶栏显示"更新于" |
| 3 | 手机打开同一地址 | 板块纵向单列、无横向滚动（B9 的前置验证） |
| 4 | 把这个网址记进打卡 | 打卡截图里有可点击的链接 |
| 5 | 顺手验证"长期可访问" | 关掉电脑、换个手机再看一次，仍能打开（B12 的前置验证） |

**注意事项**：默认域名无需备案 ✅；只要上传**静态资源目录**，不要把整个项目根目录传上去（否则 `.md` 文档也会被发布）。若 Q9 显示不能装 CLI，就在控制台手工上传，效果一样。

### 10.3 阶段 2：加上云函数（目标：真实数据）

| 步骤 | 操作 | 成功现象 |
|------|------|---------|
| 1 | 上传云函数（CLI 或控制台上传 `cloudfunctions/hot`） | 函数列表出现 `hot` |
| 2 | 配置 HTTP 访问服务，把路径指到该函数 | 拿到接口地址 |
| 3 | **先脱离前端，直接用浏览器访问 `/api/health`** | 返回 `{"ok":true,...}` —— 这一步能省掉一半联调时间 |
| 4 | 直接访问 `/api/hot` | 返回 JSON，`items` 有 40 条（或按降级规则少于 40 条） |
| 5 | 把前端 `API_BASE` 改成云函数地址并重新部署静态页 | 页面上出现真实数据、时间戳为当前时间 |
| 6 | 实测降级：临时改坏一个平台来源 | 该板块提示、其余三列正常（对齐 B10） |

### 10.4 阶段 3：全量与回滚

| 事项 | 做法 |
|------|------|
| 发布前 | 确认 `git status` 干净，提交信息写明本次改了什么 |
| 发布 | 静态页 + 云函数分别部署（顺序：先函数后页面，避免页面请求到旧接口） |
| 回滚（页面） | 静态托管有版本记录，**一键回滚到上一个正常版本** ✅ |
| 回滚（函数） | 用 Git tag/分支保留上一个可用版本，重新上传即可 |
| 验收 | 每轮发布后至少复验受影响的那几条 B 项（不要只看首页"好像正常"） |

### 10.5 每次改代码后的固定流程（四步，别跳）

1. **改**：一次只改一小块（AGENTS.md 第三节约定）
2. **本地看**：直接开文件看效果（骨架阶段甚至不用起服务）
3. **提交**：`git add` → `git commit`（标题带当天标记，如 `Day 8｜...`）→ `git push`
4. **部署 + 线上验**：部署后打开线上地址，**至少验一遍你刚改的那个行为**

> 💡 顺序不要颠倒：**先提交、再部署**。否则线上出问题时，你手上没有可回退的版本。

### 10.6 部署失败对照表

| 症状 | 最可能的原因 | 第一步查哪 |
|------|-------------|-----------|
| 打开域名 404 或空白 | 没有 `index.html`，或托管目录传错 | 托管服务的文件列表 |
| 打开的是旧版本 | 部署没成功 / CDN 缓存 | 静态托管的版本记录，再硬刷新 |
| 接口 404 | 云函数没部署成功，或 HTTP 访问路径没配 | 先直接访问 `/api/health` |
| 接口能访问，但页面报跨域 | 云函数没有返回允许来源的响应头 | 浏览器控制台那条报错的具体内容 |
| 接口 500 | 云函数内部异常 | 云函数日志（不是浏览器控制台） |
| 手机上打不开 | 链接被聊天软件截断 / 网络环境不同 | 用二维码或短链，换网络再试 |
| 部署命令直接报错 | CLI 没登录 / 环境 ID 写错 / 目录写错 | 对照 10.1 检查清单逐条过 |

### 10.7 域名与 HTTPS

| 事项 | 做法 |
|------|------|
| 默认域名 | 先用平台默认域名上线，无需域名与备案，自动 HTTPS ✅ |
| 自定义域名 | 需要域名 + 国内备案（按周计），**要提前排期**，别拖到 Day 28 |
| 同域好处 | 若页面与接口挂到同一自定义域名下，可彻底摆脱跨域问题（6.4 节那条可删） |

### 10.8 免费额度与续期（别在最后一天踩这个坑）

| 事项 | 做法 |
|------|------|
| 免费体验版口径 | 官方：每个云开发账号 1 个免费环境、每月 3,000 资源点、**单次续期 6 个月，到期前 1 个月内可续**✅；额度用尽或到期未续，环境可能停服乃至销毁 ⏳ |
| 行动项 | **开通当天就在日历里加"到期前 2 周续期"提醒**；Day 28 上线前再核一次额度余量 |
| 省钱技巧 | 刷新间隔保持 ≥10 分钟（既满足 PRD 又省额度）；静态资源尽量走 CDN 缓存 |

---

## 11. 迁移注意事项

> 回答"如果方案变成别的样子，会不会推倒重来"。结论：**不会**，因为接口契约（5.2）与数据来源（`sources.js`）都被隔离在固定位置。

### 11.1 自建 Express → CloudBase 云函数

| 事项 | 做法 |
|------|------|
| 什么不变 | 接口路径 `/api/hot`、返回结构、缓存策略**全不变**（这正是先定契约的价值） |
| 什么要改 | 入口写法：Express 的 `app.get(...)` → 云函数的"入口函数 + 事件参数" |
| 配置差异 | Express 用 `PORT` 监听；云函数由平台调度，**没有端口概念**，改为配 HTTP 触发路径 |
| 缓存差异 | Express 进程常驻，内存缓存可靠；**云函数实例可能被回收**，缓存不保证常驻 → 频繁拉取时改用数据库/云存储缓存（5.3 备注） |
| 依赖 | `express` 不再需要，保留请求库（如 axios / node-fetch） |
| 上线步骤 | 本地调通 → 上传函数 → 配 HTTP 访问 → 前端 `API_BASE` 改一行 |

### 11.2 原生前端 → Vite + React

| 事项 | 做法 |
|------|------|
| 触发条件 | ① 老师强制（Q5）；② 视图超过 3 个；③ 状态复杂到原生难维护。**三条都不满足就别迁** |
| 目录变化 | `index.html` 移到根、`app.js` 拆成 `src/` 下的组件；静态资源进 `public/` |
| 托管目录变化 | **部署要传 `dist/`（构建产物），不是源码目录** —— 最容易卡住的一步 |
| 状态管理 | 收藏读写抽成自定义 Hook，避免每个组件重复写存取逻辑 |
| 接口调用 | `API_BASE` 收进一个 `api.js`，组件只调函数 |
| 路由 | 本期无多页面；将来加路由需把未知路径回退到 `index.html` ✅ |
| 工作量 | 前端文件基本重写；**云函数、接口、数据对象、错误处理全不动** |
| 顺序 | 先跑通"空壳能构建能部署" → 再逐个搬组件 → 最后删旧文件（Git 历史可回退） |

### 11.3 本地收藏 → 云端收藏（启用 PostgreSQL）

| 步骤 | 做法 |
|------|------|
| 前置 | 必须先有登录/匿名身份（否则 `user_id` 无从取值）——这也是 PRD 把"登录"列为后续功能的原因 |
| 第一步 | 环境启用 PostgreSQL（**创建时已选 PG 类型则可直接用**，否则需新建环境并迁移托管与函数配置）🔶 |
| 第二步 | 按 5.5 节建表、加索引、配 RLS |
| 第三步 | 写迁移脚本：把浏览器里的 `hot_favorites_v1` 读出来逐条 POST，**成功后本地留一份只读备份** |
| 第四步 | 去重：靠 `unique(user_id, platform, title)` 兜住；冲突按"更新备注"处理 |
| 第五步 | 前端改为"优先读云端、不可用时回退本地"，并提示"收藏已同步到云端" |
| 回退 | 接口出问题时仍退回纯本地——**本地存储的代码不要删**，留作降级路径 |

### 11.4 换数据来源（最常见的变更）

| 事项 | 做法 |
|------|------|
| 只改一处 | 来源的获取与解析集中在 `cloudfunctions/hot/sources.js` |
| 契约不变 | 只要最终仍输出 `HotItem`，前端一行都不用改 |
| 密钥 | 新源如需 Key → 进云端环境变量，不进代码 |
| 验证 | 改完必须重跑 B10（单平台失败降级）与 B1（40 条真实数据） |

### 11.5 环境 ID 与配置漂移

| 事项 | 做法 |
|------|------|
| 环境 ID 变了 | 改 `cloudbaserc.json` + 前端 `API_BASE`，共 2 处 |
| 建议 | 在配置里就地写一行注释"换环境只改这里"，避免以后全局搜索 |

---

## 12. 方案变化影响面

| 如果方案变成… | 受影响的文件 | 影响程度 |
|--------------|-------------|---------|
| 前端改上 Vite + React | 前端文件重写、新增构建配置、托管目录改 `dist/`；后端与接口不动 | 中 |
| 收藏改为云端同步 | 环境启用 PG、新增 4 个接口、前端读写改造、PRD 需重议范围 | 大 |
| 数据库类型选成"文档型"而非 PostgreSQL | 5.5 表结构改为集合、11.3 脚本调整、环境无需重建 | 小 |
| 数据源换成第三方付费 API | 只改 `sources.js` + 云端环境变量加 Key | 小 |
| 平台从 4 个改为 6 个 | `sources.js`、`app.js`（列布局）、`styles.css`（断点）、PRD 范围与验收 | 中 |
| 加登录注册 | 需身份认证服务 + PG 表加 `user_id` 约束、前端多登录界面、PRD 大改 | 大 |
| 换部署平台（如 Vercel / 自建服务器） | `cloudbaserc.json` 与部署命令、`API_BASE`；云函数需改为普通服务 | 中 |
| 刷新间隔由 10 分钟改为 1 分钟 | `CACHE_TTL_MINUTES` + 上游限流风险上升 | 小 |

**结论**：把"数据来源"锁在 `sources.js`、把"接口契约"锁在 5.2 节之后，**大部分变化都被关在云函数里**，前端与文档都不用动。

---

## 13. 技术栈科普（本项目取舍一览）

**技术栈** = 做这个产品用到的所有技术组合，像"盖房子的材料清单"。

| 层 | 常见技术 | 有什么用 | 优势 | 劣势 | 本项目 |
|----|---------|---------|------|------|--------|
| 结构 | HTML | 页面骨架 | 必备、简单 | 单靠自己不好看 | ✅ 用 |
| 样式 | CSS | 排版、配色、响应式 | 控制外观 | 大项目难维护 | ✅ 用 |
| 交互 | 原生 JavaScript | 渲染、弹层、收藏、刷新 | 零依赖、易调试 | 复杂页面会乱 | ✅ 用 |
| 框架 | React / Vue（+ Vite） | 组件化开发复杂界面 | 生态大、易维护 | 学习与构建成本高 | ⏸️ **本期不用**，留升级路径（11.2） |
| 后端 | Node.js + Express | 提供接口、处理数据 | 与前端同语言 | 需自己运维、机器要常开 | 🔶 仅本地调试 |
| 后端（托管） | **CloudBase Node.js 云函数** | 取数、统一、缓存、出接口 | 免运维、自动扩缩容、自动 HTTPS | 冷启动、配置类报错不友好 | ✅ 本期主用 |
| 后端（其他语言） | Python / Java / Go | 同上 | 生态成熟 | 零基础要多学一门语言 | ❌ 不用 |
| 数据库 | MySQL / MongoDB / SQLite | 长期存数据 | 能存海量与关系型数据 | 要设计表、要运维 | ❌ 本期不用 |
| 数据库（云） | **CloudBase PostgreSQL** | 云端存收藏，支持 RLS 与自动接口 | 环境创建即得、标准 SQL | 本期无账号体系用不上 | ⏸️ 二期启用（5.5、11.3） |
| 本地存储 | 浏览器 localStorage | 存收藏与备注 | 免登录、零成本、零隐私风险 | 换设备就没有 | ✅ 本期用 |
| 部署 | 静态托管（CloudBase） | 把网页发布到全网 | 免服务器、自动 HTTPS + CDN、可回滚 | 只适合静态资源 | ✅ 本期用 |
| 版本管理 | Git + GitHub | 记录修改、可回退 | 必备技能 | 要学基本命令 | ✅ 已就绪 |

**一句话**：本项目技术栈 = **HTML + CSS + 原生 JS + CloudBase 云函数 + CloudBase 静态托管 + localStorage + Git**；数据库先"占好位置"，等有了登录再开。

---

## 14. 技术层面的"本期不做"

- 不做前端构建工具链（Vite/Webpack）、不上 React/Vue（3.3 取舍一已说明，留迁移路径）
- 不引入数据库、不做账号与登录鉴权（PostgreSQL 仅预留结构，不建表不连库）
- 不做服务端渲染（SSR）、不做多页面路由
- 不做自动化测试与 CI/CD（本期靠人工按 PRD B1–B12 逐条点）——**明确因为时间限制而砍**
- 不做容器化、不做自建服务器运维
- 不做数据落库归档（热榜是"看一眼就够"的时鲜数据）

---

## 15. 与 PRD 验收标准的对应

| PRD 验收项 | 本方案如何支撑 |
|-----------|--------------|
| A1、A2 骨架结构（Day 7） | 阶段 1：原生页面 + 静态托管上线，用本地样本数据（10.2） |
| B1 40 条真实数据 | 阶段 2：云函数服务端取回 + 统一字段（7.1 ①②） |
| B2 首屏 ≤2 秒 | 服务端缓存 + 静态托管 CDN + 前端一次请求取全量（5.3、7.1 ⑤） |
| B3、B4 详情与跳原文 | 前端弹层 + `url` 字段直接跳转（5.1） |
| B5–B7 收藏、备注、本地持久化 | localStorage 读写（5.4），明确"不回传服务器" |
| B8 刷新 | `CACHE_TTL_MINUTES=10` + 前端手动刷新（5.3、6.1） |
| B9 移动端无横向滚动 | 前端响应式布局（8.3） |
| B10 单平台失败降级 | `platforms[].ok` + 前端按板块降级（5.2、8.1、8.3） |
| B11 无登录无广告 | 无账号体系、无第三方广告脚本、不收集用户数据 |
| B12 30 秒看完 + 长期可访问 | 一屏四列 + 静态托管长期托管（10.2 第 5 步已提前验证） |

---

## 16. 参考资料（公开来源，访问日期 2026-09-20）

| 序号 | 资料 | 用到了什么 | 类型 |
|------|------|-----------|------|
| 1 | 腾讯云开发 CloudBase 官网 https://tcb.cloud.tencent.com/ | 平台能力概览、套餐档位 | ✅ 已核实 |
| 2 | CloudBase 定价页 https://cloudbase.cloud.tencent.com/pricing | 免费体验版：1 个免费环境、3,000 资源点/月、单次续期 6 个月 | ✅ 已核实（具体权益⏳以购买页为准） |
| 3 | CloudBase 开发文档中心 https://docs.cloudbase.net/ | 数据库双引擎（PostgreSQL + 文档型）、云函数与静态托管能力、框架接入 | ✅ 已核实 |
| 4 | 腾讯云文档《云开发 CloudBase · 购买指南》https://cloud.tencent.com/document/buy-guide/876/127357 | **环境创建时可选数据库类型、地域差异、事后不可混用** | ✅ 已核实（3.3 取舍二的关键依据） |
| 5 | 腾讯云《静态网站托管》产品页 https://cloud.tencent.com/product/wh | 免费默认域名、自定义域名免费 SSL、强制 HTTPS、CDN 加速、CLI 部署 | ✅ 已核实（免费流量口径可能已变 ⏳） |
| 6 | CloudBase 官方博客《PostgreSQL 支持》https://cloudbase.cloud.tencent.com/en/blog/2026/08/10/cloudbase-postgresql | `postgresql://` 直连、RLS、自动生成 RESTful 接口、兼容 Supabase SDK、数据库分支 | ✅ 已核实 |
| 7 | 腾讯云《云开发 CloudBase》产品页 https://cloud.tencent.com/product/tcb | 云函数/云托管形态、Web 应用托管（React/Vue、HTTPS + CDN、Git 构建） | ✅ 已核实 |
| 8 | 本项目 research.md、PRD.md v2 | 产品方向、MVP 范围、B1–B12 验收标准 | 本项目文档 |

**仍不确定、需人工核实的信息（⏳）**

| 序号 | 不确定项 | 何时确认 |
|------|---------|---------|
| 1 | 4 个平台是否有可用、合规的公开数据来源（决定 B1） | **Day 8 开始前**（最高优先级） |
| 2 | CloudBase 环境是否已开通、环境 ID、地域、数据库类型 | 阶段 1 部署前 |
| 3 | CloudBase CLI 能否安装（Q9）；若不能，控制台上传的具体路径 | 阶段 1 部署前 |
| 4 | 云函数 HTTP 访问的完整域名格式与跨域配置方式 | 阶段 2 开始前 |
| 5 | 免费额度当前口径与续期规则 | 开通当天 |
| 6 | 腾讯云能否实名、能否绑支付方式（方案 B 的硬门槛） | 尽早确认 |
| 7 | 课程/老师是否强制 React/Vite（影响 3.3 取舍一） | 尽早问老师 |
| 8 | 页面与接口是否同域（影响是否需要 CORS） | 阶段 2 联调时 |
| 9 | 云函数实例回收频率是否影响缓存命中 | 阶段 3 实测 |
